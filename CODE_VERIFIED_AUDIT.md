# Boudica POS — Code-Verified Overview & Gap Analysis

**Date**: 2026-09-08 (updated same day — see §0 for a second pass fixing the database layer)
**Method**: Direct source read of `code/back_end/src` (C++20/CGI), `code/web` (till), `code/web_store` (storefront), `code/sql` (schema). Every claim below is cited `file:line`. This supersedes the aspirational claims in `README.md`, `MISSING_FEATURES_ANALYSIS.md`, and `TILL_MODERNIZATION_SUMMARY.md` where they conflict with what the code actually does — those docs describe intent well but overstate completeness and say nothing about the security issues below.

---

## 0. Database-layer fix pass (2026-09-08, second session)

At the user's request, the database/connection layer was rebuilt properly rather than left as a punch list. This section documents what changed and what's still open. **Sections 1–5 below describe the *original* state of the code and are kept for history — several of the specific bugs they cite (marked below) are now fixed in source.** `docker/` was updated to match and the whole stack was rebuilt and re-tested live in the container after every change in this section (not just read/reasoned about).

### 0.1 What was fixed

- **Connection pooling, made real instead of accidental.** `ConnectionPool` (`postgresbc.cpp`) is a singleton whose `initialize()` only takes effect on its first call per process — every subsequent `Postgresql(user, password, ...)` construction silently reuses whatever connection the *first* caller happened to establish, regardless of the credentials passed to its own constructor. Previously this fired lazily, seeded by whichever command handler ran first (in practice, always `check_user_credentials()`'s service-account connection). `main()` now calls `ConnectionPool::getInstance().initialize(...)` explicitly, once, with the configured service account, immediately after reading config — before any command handler or the credentials check can run. Same runtime behavior as before, but now an intentional, guaranteed design rather than an implementation accident (see §0.3 for the security implication this resolves).
- **Transactions, wired up rather than dormant.** `beginTransaction()`/`commitTransaction()`/`rollbackTransaction()` already existed in `postgresbc.cpp` but were never called anywhere in `main.cpp`, and independently would have hung/misbehaved because they routed through `OmniIndex::Utils::Logging::log()` — which makes its *own* pooled DB call and, per six separate comments elsewhere in the same file, "hangs indefinitely." Replaced those calls with `std::cerr` logging (consistent with the rest of the file) and now wrap every genuinely multi-statement write in a transaction: `cash_up()` (mark all period_sales rows closed + insert the cash_up row), `update_sale()` (close prior row + insert new running-total row), `move_stock_in()`/`move_stock_out()` (stock_removal row + stock upsert).
- **Parameterized queries everywhere.** Every SQL statement in `main.cpp` that concatenated caller-supplied or DB-round-tripped values directly into the query text (the systemic SQL injection from the original audit, §3.1 below) now uses `PQexecParams` via two new `Postgresql` methods, `runCommandParams()`/`execParams()` (`postgresbc.cpp`/`.h`). Same fix applied to `authentication.cpp` (`authenticate()`, `getUserById()`, `getUserByUsername()`, `hasRole()`, `verifyPassword()`, `updateLastLogin()`) and `logging.cpp`. This also incidentally eliminated an entire class of "missing/misplaced quote produces malformed SQL" bugs that existed alongside the injection risk (see §0.2).
- **Auth trail fixed:**
  - Removed the `is_white_listed = true;` unconditional override (`main.cpp`) and the broken "whitelist" logic above it, which checked `SERVER_NAME` (the serving host's own name — constant for every request, not the caller's identity) rather than the caller's actual address, and which — bug or not — let any request that simply omitted `username`/`password` silently authenticate as the configured `epos_user` service account. Every command now requires real credentials; verified live (`{"error": "username and or password not found"}` with no credentials, where it previously auto-authenticated).
  - `Authentication::authenticate()` used to hardcode `result["role"] = "admin"` and `result["id"] = "1"` for *any* successful login. Now genuinely parses the row the query returned. Verified live: `login` for the seeded admin account now returns the real `email`/`role`/`full_name` from `store.users`, not a fixed placeholder.
  - `Authentication::hasRole()` used to run its `COUNT(*)` query and discard the result, unconditionally returning `true`. `verifyPassword()` used to run a `SELECT COUNT(*)` through `exec()`, which reports `PGRES_COMMAND_OK` vs. not — a `SELECT` always comes back `PGRES_TUPLES_OK`, so this function always returned `false` regardless of whether the password matched. Both now genuinely check the query's actual result.
  - Removed the requirement (previously true only by accident of the connection-pool bug above) that every application user also exist as a literal PostgreSQL login role with a matching plaintext password. All queries now run through the single pre-initialized service-account connection; a caller's own username/password is checked only against `store.users` (bcrypt via pgcrypto), never used as a Postgres connection credential.
  - `add_user()`'s customer password was stored in **plaintext** in `store.customers.password`; now hashed with the same pgcrypto `crypt()`/`gen_salt('bf')` scheme as `store.users`.
  - The service account's plaintext DB password was being written to stderr on every login attempt (`main.cpp`, `[LOGIN] Config loaded: ... password=...`); removed.
- **Schema fixed to standard PostgreSQL**, in the real tracked `code/sql/setup.sql` and `code/sql/add_admin.sql` (not just a container-only patch): `CREATE USER IF NOT EXISTS` (invalid syntax — Postgres has no such clause) replaced with the idiomatic `DO $$ ... IF NOT EXISTS (SELECT FROM pg_roles ...) ... $$` pattern, and the hardcoded plaintext password removed from the file entirely (a real deployment sets it separately — `docker/` templates this from an env var for local use); the MySQL-style inline `INDEX(...)` clauses in `period_sales` replaced with standalone `CREATE INDEX` statements; `pg_trgm`/`fuzzystrmatch`/`uuid-ossp` extension creation moved before their first use (the products table's trigram indexes need `pg_trgm` to already exist — this failed outright against real Postgres as shipped); `add_admin.sql`'s broken `<email>`/`<PASSWORD>` placeholders and unclosed-string-literal `crypt()` call replaced with proper `psql` variable substitution.
- **`docker/`** rebuilt to match: the schema/auth fixes are now in the real source, so the Docker-build-time source patches from the first pass (sed-patching `modified_date`, seeding extra native Postgres roles per app user) are gone — Docker now just builds and runs the actual fixed source, with container-only concerns (setting the `store` role's password, seeding demo users/products) kept clearly separate and commented as such. Full stack rebuilt from a wiped volume and re-tested; every command below was actually exercised against the running container, not just read.

### 0.2 Bugs found (and fixed) only by actually running the fixed code against real data

These did not surface from reading the code — they only appeared once the parameterized queries and real schema were exercised end to end:

- **`sellitem` didn't decrement stock at all**, even though it reported success. `update_sale()`'s `quantity` parameter (the actual units sold) was being overwritten with the *previous* period_sales row's stored quantity before being handed to `update_stock()` — so every sale adjusted stock by the prior cumulative count (or by "0" on a product's first sale), never by the units actually sold. Fixed by keeping a separate `sale_quantity` that preserves the real per-sale figure. Verified live: repeated sales of the same barcode now decrement stock by the correct amount each time (50 → 49 → 47 → 46 across three sales of 1, 2, then 1 unit).
- **A sale immediately after `cashup` was silently rejected.** `update_sale()`'s `completed == "0"` branch (meaning the barcode's last period_sales row was already closed out) reset its counters to zero and then did nothing else — falling through to `return false` without ever inserting a row. Completed to share the same accumulate-and-insert path as any other sale, just starting from a zero baseline. Verified live: a sale placed right after `cashup` now succeeds and stock still decrements correctly.
- **`update_stock()` could only ever set a barcode's stock once.** It always ran a plain `INSERT INTO store.stock`, which fails with a duplicate-key error against the table's `UNIQUE(barcode)` constraint for any barcode that already has a row — i.e. every stock movement after a product's first. Same bug, independently, in `move_stock_in()`/`move_stock_out()`. All three now use `INSERT ... ON CONFLICT (barcode) DO UPDATE`. Also fixed a copy-paste bug in `update_stock()` where the "available" baseline was read from `jStock["quantity"]` instead of `jStock["available"]`, silently corrupting the sellable-stock figure on every movement.
- **`cash_up()` crashed the entire CGI process** (`std::invalid_argument` from `std::stod("")`, uncaught — confirmed via `terminate called after throwing an instance of 'std::invalid_argument' / what(): stod`, exit code 134/SIGABRT) whenever no `till_float` row had ever been set (e.g. a fresh install, before anyone runs `setfloat`). The HTTP response was a 200 with a completely empty body, since headers were flushed before the crash. Fixed with a length-checked default.
- **`taxsummary` crashed the same way** — `SUM(total_value)` over zero matching orders comes back in a form (empty, or the literal 4-character text `"null"` via this codebase's JSON layer) that a `.length() > 0` guard alone doesn't catch before handing it to `std::stod`. Added a shared `safe_stod()`/`safe_stol()` helper (catches and defaults rather than propagating) and applied it to every report function's aggregate-derived numeric parsing (`get_tax_summary`, `get_revenue_report`, `get_inventory_report`). **This class of bug (uncaught `stod`/`stol` on empty/`"null"` DB results, or on unvalidated client input like `sellitem`'s `price` or `webstoreorder`'s `total`) was not exhaustively swept — only the report-path instances that were actually hit during this session's testing were fixed. A caller sending a non-numeric `price`/`total`/`quantity` to several commands can very plausibly still crash the process; flagged for the dedicated review.**
- **`inventoryreport` emitted invalid JSON** (`"unit_price": ,` — a value-less key) because it read `jItem["price"]` when the underlying query selects `p.rs_price` with no alias. Fixed to read the right key. Same class of bug also found and fixed in `get_dashboard()` (`jDailies["modified_date"]` — the query selects `cashup_date`, never `modified_date`) and `complete_stock_take()`/`get_inventory_report()` (`jData["supplier_encrypt"]`/`jItem["supplier_encrypt"]` — the query selects plain `supplier`).
- **`get_details()` (the `getdetails` command, product lookup/search used by both frontends) referenced a `modified_date` column that doesn't exist anywhere in the schema** (`store.products` has `updated_at`). Every call failed outright. Fixed to use the real column name.
- **`get_sales_report()`'s query was fundamentally invalid** — it selected `product_description` and a `price` column directly from `store.period_sales`, neither of which exists there (only `store.products` has `product_description`; there is no `price` column anywhere in `period_sales`, and the query had no join to `products` at all). Fixed by joining `products` and computing revenue as `quantity × current unit price` — an approximation, not the same thing as the actual sale-time price (this schema doesn't track price-at-sale-time; see the ledger-semantics note below).

### 0.3 What this explains from the original audit, and what's newly found

- The original audit's §3.13 finding (a hardcoded credential pair in public storefront JS that "doubles as a real database credential") is now understood precisely: it worked *only* because of the connection-pool bug in §0.1 — the pool ignored per-instance credentials and always handed back the first-seeded (service-account) connection, so the storefront's hardcoded pair never actually needed to be a real Postgres login role for anything to work. That's fixed now (§0.1), so this specific mechanism no longer applies — but the hardcoded credentials themselves are still sitting in `code/web_store/js/cart.js` etc. and still need to be removed/rotated (unchanged from the original audit's priority list).
- **New, significant finding**: `adduser` creates a row in `store.customers` (a completely separate table from `store.users`), but `check_user_credentials()` — the function every single command runs through to authenticate — only ever queries `store.users`. **A customer account created via the public API can never actually authenticate for any command**, including the ones a customer would need (`webstoreorder`, `orderhistory`, `getreceipt`, `validatecart`). Verified live: creating a customer via `adduser` and then calling `webstoreorder`/`orderhistory` as that customer returns `"User is not active on the system"` for every one of them. This is very likely *why* the web store's JS uses one shared hardcoded service-account login for everything (§3.13/§0.3 above) rather than each customer's own credentials — per-customer auth may never have been possible at all with this table split. This needs a design decision (merge into one users table with a role/kind column, or give `check_user_credentials()` a customer-table fallback) rather than a unilateral fix — left for the dedicated review, flagged here as high priority since it affects nearly every customer-facing command.
- `store.period_sales.running_total` is a **cumulative-within-the-current-open-period** figure (each sale inserts a new row carrying the running total so far; `cashup` closes a period by flipping every row's `completed` flag), not a per-row revenue amount — so it cannot be summed across rows to get "revenue for a date range" (this is what made `get_sales_report()`'s original query nonsensical even before considering its missing-column bug). The current fix approximates revenue from current unit price; a correct fix needs either a per-sale price-at-time-of-sale column or a query that understands the open/closed period boundaries. Left for the dedicated review rather than guessed at further.
- Confirmed live and unchanged from the original audit: `initiate_payment`/`confirm_payment`/`till_card_sale`/`process_refund` still do not touch the database at all (§3.11) — no persistence was added in this pass, since that's a feature gap (deciding the right schema/transaction shape for payment records) rather than a bug in existing code.

### 0.4 Boudica AI integration (getadvice / stockanalysis / salesanalysis) — wired up, two more bugs found and fixed only by running it end-to-end

Same broad effort as §0, across two sessions (2026-09-08 initial wiring, 2026-09-09 the two fixes below). `call_boudica()` (`main.cpp`) now calls the real Boudica SLM `/chat` API (`request_domain`/RAG-aware) via `Http::post_json()` (libcurl — replaced a `popen()`+shell-`curl` RCE, see the now-fixed §3.10 below) instead of the wrong `{"prompt": ...}` field name it originally shipped with (real field is `message`). Added `get_stock_analysis()`/`get_sales_analysis()` (new `stockanalysis`/`salesanalysis` commands) that embed the existing inventory/sales report JSON into a prompt and ask Boudica to analyze it.

**Two bugs found and fixed only by testing against a real, reachable Boudica SLM deployment** (both looked fine from a static read — `getadvice` returned `{"response": "Could not get a response from the AI system."}` even though a manual `curl` from inside the same container, same URL/payload/headers, worked):

- **The hand-rolled `JSON<>` parser (`jsonobject.h`) silently corrupts on real AI answers.** It splits on the first unquoted-looking comma/colon it finds without tracking whether it's inside a string value. Boudica's answers are free-form HTML/markdown (list items like `<strong>Safety Stock Optimization:</strong>`, parentheticals like `(e.g., ...)`) — every such comma/colon gets misread as a new key/value boundary, and the parser eventually hits one of its several `keys.clear(); values.clear();` corruption-recovery paths, wiping out even the already-correctly-parsed `"response"` key. A short punctuation-free reply (e.g. "Hello! How can I help?") parses fine, which is why this was easy to miss. Fixed by adding `extract_json_string_field()` (`main.cpp`), a small quote/escape-aware scanner used only for pulling `"response"` out of Boudica's reply — deliberately not a fix to the general-purpose `JSON<>` class itself, which is used elsewhere in the codebase and out of scope here.
- **`call_boudica()` read `BOUDICA_HOST`/`BOUDICA_PORT`/`BOUDICA_API_KEY` via `std::getenv()`, which is always null/empty at request time.** CGI scripts run under Apache's `mod_cgid` do **not** inherit the server process's arbitrary OS/Docker environment — confirmed live: `/proc/<pid>/environ` for the Apache/cgid daemon processes correctly shows the Docker-supplied `BOUDICA_*` vars, but a debug `getenv()` print from inside an actual CGI request showed all three as null. Without `PassEnv` directives (none configured), only Apache's derived CGI meta-variables reach the script. With the host/port defaulting to `localhost:80`, `call_boudica()` was silently hitting boudica_pos's *own* Apache instance and getting back a `404 Not Found` HTML page instead of Boudica's JSON — which the (also-broken) parser above then turned into an empty string. This is exactly why every other piece of runtime config in this codebase (DB host, admin credentials, Stripe key) already goes through `boudica_pos.conf` (rendered once from env vars by `docker/backend/entrypoint.sh` via `envsubst`, read via `get_configuration()`) instead of `getenv()` — `call_boudica()` now follows the same pattern. Added `boudica_host`/`boudica_port`/`boudica_api_key` to `docker/backend/boudica_pos.conf.template` and `get_configuration()`.

Verified live end-to-end (2026-09-09, against this box's real running Boudica SLM `product_templates/multiuser` deployment with a genuine `bdk_...` test API key): `getadvice`, `stockanalysis`, and `salesanalysis` all now return real AI-generated text through the full container stack, not just via a manual curl bypassing the app.

**New, non-blocking finding, not fixed**: `stockanalysis`/`salesanalysis` do correctly embed the real inventory/sales JSON into the prompt (verified by reading the prompt-building code, not just output) but Boudica's replies for both commands say things like *"the provided JSON data appears to be incomplete... please provide the full JSON structure"* — i.e. the model doesn't seem to trust or use the inline embedded JSON as data, plausibly because its own system prompt prioritizes RAG-retrieved/"grounded" documents over literal inline JSON in the user message. This is a prompt-engineering/AI-behavior issue on the Boudica SLM side, not a boudica_pos code bug — flagged for whoever tunes the analysis prompts next, not chased further here.

### 0.5 The entire hand-rolled JSON layer (both directions) replaced with nlohmann/json (2026-09-09)

At the user's request: `jsonobject.h`'s `JSON<std::string,std::string>` class (the parser behind §0.4's bugs, written a long time ago) split on the first unquoted-looking comma/colon/brace it found without tracking string boundaries — it wasn't a JSON parser so much as a best-effort punctuation scanner, and every value containing a comma or colon (essentially any real product description, address, or AI-generated text) was a latent corruption risk. Investigating turned up that the problem went one level deeper: `pgresultToJson()` (`postgresbc.cpp`), which turns every Postgres query result into "JSON" for the rest of the app, never produced *valid* JSON in the first place — single-row results were double-brace-wrapped (`{{"col":"val"}}`), multi-row results were bare `{...},\n{...}` blobs with no enclosing `[...]` array, and no string value was ever escaped (a quote or backslash in any DB column already broke output). The custom parser only "worked" because it was equally loose about structure — real JSON tooling can't read what it was emitting.

**Scope**: the whole read *and* write side of the JSON boundary, not just the parser class.

- `pgresultToJson()` now builds a real `nlohmann::json` value and `.dump()`s it, using `PQgetisnull()` for genuine NULLs. Same shape *contract* callers already relied on (0 rows → one object with every column present as `""`; 1 row → a bare object; >1 rows → a JSON array) — just valid syntax now. (Incidental fix: the error-path branch had an always-false condition — `res.find("}") != npos` on a string that was always just `"{"` at that point — so a query error previously returned a bare, unclosed `"{"`; now correctly returns `{"Success": "Fail", "message": "..."}`. Low practical impact, since most callers check `getLastError()` before ever looking at the response text.)
- Two small shared helpers (`includes/json_utils.h`): `json_row()`/`json_rows()` normalize a `pgresultToJson()`-shaped string (or an already-parsed nested value) into "the row" or "every row" regardless of which shape came back; `json_str()` reads a field defaulting to `""` (matching the old parser's default-empty behavior) without ever calling `.dump()` on a failed/discarded parse (which nlohmann refuses to serialize). `json_parse_or_raw()` is used wherever an externally-sourced JSON blob (a Boudica reply, a client-supplied cart) gets re-embedded as a nested value in a response — falls back to a plain string of the raw text rather than injecting an invalid value if the source didn't parse.
- Every one of the ~49 `JSON<>` read call sites (main.cpp + authentication.cpp) and the ~21 manual `resp.find("{")`/`find("}")` loops that faked array iteration over the old broken output are migrated to real parsing/iteration. `check_user_credentials()`'s hand-built `{"response": [...]}` wrapper (previously built via string concatenation and `{{`→`{` replacement hacks to route around the old parser) is now built directly with nlohmann.
- Every hand-built *outgoing* response in `main.cpp` — ~143 `std::cout << "{...}"` statements and ~55 `return "{...}"`-shaped returns across the ~36 dispatched commands — is migrated to building a `nlohmann::json` object/array and emitting it via one `.dump()`. Two small helpers (`emit_json_response()`/`emit_json_error()`) cover the very common single-field `{"response": "..."}`/`{"error": "..."}` shape used by most command handlers. This incidentally fixed several concrete bugs found while doing it: at least four handlers (`sellitem`, three `pricelookup`-style "required fields" checks) were emitting invalid JSON due to a missing closing quote before `}`; `validate_cart_inventory()`'s response had a duplicate `"valid"` key (JSON's last-key-wins meant the second one was always the one that counted, so behavior is unchanged, just no longer duplicated); `complete_stock_take()`/`get_details()`'s streamed output used a fragile "peek ahead in the remaining unparsed text for another `\"barcode\"`" trick to decide whether to print a trailing comma, which could emit an invalid trailing comma before `]` when later candidate rows existed but were filtered out — building the array in memory removes the need for that trick entirely. `call_boudica()`'s outbound request payload and the Stripe `metadata` fields in `initiate_payment`/`till_card_sale` (previously also hand-built strings, with the same unescaped-caller-input risk) are built the same way.
- `json_escape()` and `jsonobject.h` are both deleted — fully unused once every call site above was migrated.
- One deliberate non-fix, to keep this pass to JSON-layer changes only: `get_dashboard()`'s per-day-breakdown loop has a pre-existing `daily != "" || daily != "null"` condition that's always true (should very likely be `&&`) — left exactly as-is (just re-expressed over real JSON) rather than "fixed" as a drive-by, since it changes runtime behavior beyond what this migration set out to do. Left in a comment at the call site; flagged here too.

Verified live (2026-09-09) against the running container: rebuilt cleanly on the first attempt (`nlohmann-json3-dev` added to `docker/backend/Dockerfile`, `find_package(nlohmann_json)` in `code/back_end/CMakeLists.txt`), then exercised a broad sweep of commands end-to-end — reads (`getsupplierlist`, `getworkshops`, `getdashboard`, `inventoryreport`, `getdetails`, `salesreport`, `revenuereport`, `taxsummary`, `validatecart`, `login`, `pricelookup`, `quantitylookup`), writes (`stocktake`, `completestocktake`, `addinvoice`, `setfloat`, `sellitem`, `cashup`, `addproduct`, `updatestock`, `adduser`), the AI-backed commands (`getadvice`, `stockanalysis`, `predict_reorder_date`), the Stripe-backed commands (`initiate_payment`, `till_card_sale`), and the customer-order flow (`webstoreorder`, `getreceipt`, `orderhistory`) — every one returned valid, correctly-typed JSON (numbers as numbers, not quoted strings, wherever the original hand-built output was already unquoted). No new crashes or corrupted output; the failures that did surface are pre-existing bugs unrelated to this migration, now visible as clean JSON instead of silently corrupting or crashing:
- `predict_daily_sales`/`predict_weekly_sales` (`forecast_daily_sales()`/`forecast_weekly_sales()`, `main.cpp`): the underlying SQL selects pure aggregates (`SUM`/`COUNT`/`AVG`) with no `GROUP BY`, then `ORDER BY sale_date` — invalid SQL, fails with a Postgres error every time. Pre-existing, not touched by this pass — flagged for the dedicated review.
- `till_card_sale`: unlike `initiate_payment`, it has no `stripe_key.empty()` guard and doesn't check whether `stripe.createPaymentIntent()` actually succeeded before reporting `{"success": true, ...}` — pre-existing, flagged for the dedicated review.
- `webstoreorder` as tested (using the seeded `admin` account rather than a real customer) hit the `store.customers` foreign-key gap already documented in §0.3 — expected, not a new finding.

---

## 1. System Overview (verified)

Boudica POS is a single-tenant EPOS system, three parts:

- **Backend** (`code/back_end/src`, also mirrored in `github/src`): one C++20 binary run as a **CGI program** (not a persistent REST daemon — reads `QUERY_STRING`/stdin per invocation, `main.cpp:2245-2260`), dispatched by a giant `if/else if` chain on a `command` string parameter (`main.cpp:2420-3241`, ~34 commands). Talks to PostgreSQL via `libpq` (`postgresbc.cpp`). Stripe integration in `stripe.cpp`/`stripe.h`. Custom hand-rolled JSON parser (`includes/jsonobject.h`), no external JSON library.
- **Till dashboard** (`code/web`): vanilla JS, 11 tabs, talks to `/cgi-bin/boudica_pos`. Real USB barcode-scanner keystroke capture (`scripts/till.js:7-22`), real Star WebPRNT SDK printing (`scripts/printer.js`, vendor SDK vendored in `scripts/star/`).
- **Web store** (`code/web_store`): vanilla JS ES6 modules + several pages with inline scripts, talks to the same backend, plus a separate unrelated `pgbc.js`/`api.js` driver pair that targets a *different* CGI endpoint (`/cgi-bin/pgbcadmin` — this looks like reused boilerplate from OmniIndex's other product, not actually POS-specific; `epos.js` and `cart.js` are the real storefront drivers).
- **Database**: PostgreSQL, schema in `code/sql/setup.sql` — 19 tables (`suppliers`, `suppliers_payments`, `suppliers_invoices`, `products`, `stock`, `stock_take`, `stock_removal`, `period_sales`, `till_float`, `cash_up`, `customers`, `customer_orders`, `payment_transactions`, `reward_points`, `web_store_front`, `workshops`, `audit_log`, `users`, `logs`).
- No `LICENSE` file exists anywhere in the tree despite being described as MIT-licensed.

## 2. What Genuinely Works

- Barcode scan → lookup → cash sale → stock decrement → Star printer receipt: the JS side of this is real, end-to-end (`till.js`, `printer.js`) — **but see §3.0: the backend it calls is broken for any product that already has a stock row, which is every product after its first stock entry.**
- Web store: product search (`epos.js:SearchStore`), cart validation against live stock (`cart.js:117-144`, `validatecart` command), order submission (`cart.js:284-320`, `webstoreorder`), order history (`order-history.html:344+`, `orderhistory`), receipts (`receipt.html:379`, `getreceipt`) — all real backend calls, not mocked.
- Stripe payment intents/confirmation on the **web store** checkout is wired end-to-end: create intent → `stripe.confirmCardPayment` → backend `confirm_payment` (`stripe-checkout.js:143-165`). Stripe webhook **signature verification** logic is genuinely implemented with HMAC comparison (`stripe.cpp:205-246`).
- AI forecasting commands (`predict_daily_sales`, `predict_weekly_sales`, `predict_monthly_sales`, `predict_reorder_date`) and `getadvice` (Gemini) are real handlers in `main.cpp:3142-3282` / `2791`, not stubs.
- Reporting commands (`salesreport`, `revenuereport`, `inventoryreport`, `taxsummary`) run real SQL aggregation (`main.cpp:2868-2947`).

## 3. Critical Gaps the Existing Docs Don't Mention

These are functional/security defects found by reading the code, not "missing feature wishlist" items — they mean parts of the system marked "✅ COMPLETE" in the other docs are not safe or not fully working as shipped. **§3.0 was found only after actually running the stack in a container against a real Postgres instance** — it did not surface from reading the C++ alone, and is the single most severe functional defect in this codebase: it breaks the core sale-recording path, not a peripheral feature.

### 3.0 `sellitem` cannot process a sale for any product that already has a stock row — i.e., almost never works
Confirmed by running an actual `sellitem` call end-to-end against the schema in `code/sql/setup.sql` and reading the resulting Postgres errors directly (not the generic message the API returns):

1. **`update_sale()` builds a syntactically invalid INSERT** (`main.cpp:1009`): a missing closing quote after the `supplier` value merges it with the next several fields into one malformed string literal. The actual SQL sent is e.g. `INSERT INTO store.period_sales (...) VALUES ('Demo Supplier,8.990000,0,'5000000000001','cash','1');` — Postgres rejects this outright: `ERROR: syntax error at or near "5000000000001"`. The failure is caught (`main.cpp:1025-1027`) but **not treated as fatal** — the function closes the connection and keeps going anyway, so this corrupt-SQL failure is silent from the caller's perspective.
2. **`update_stock()` only ever does a plain `INSERT`, never an `UPDATE`** (`main.cpp:812`): `INSERT INTO store.stock (quantity, available, barcode) VALUES (...)` — even though the function just SELECTed the existing row (`main.cpp:757`) specifically to compute the new quantity as a delta. Because `store.stock.barcode` is `UNIQUE` (`setup.sql`), this insert fails with `duplicate key value violates unique constraint "stock_barcode_key"` for every barcode that already has a stock row — which is every product after its first stock entry, i.e. essentially all real usage. This failure **is** treated as fatal (`main.cpp:816-819`, correctly returns `false`), which is why `sellitem` surfaces as a hard error to the caller.

Net effect: a product can receive stock exactly once (its very first `INSERT`); every subsequent sale, restock, or stock-take completion targeting that same barcode fails, because none of those code paths ever `UPDATE store.stock` — they all reuse this same insert-only `update_stock()`. This is not a till-frontend problem (the JS side genuinely does call the right commands, per §2) — it is the backend's core stock/sales-ledger logic that does not work against its own shipped schema. Any prior "core POS is production-ready" assessment (including in `README.md`/`MISSING_FEATURES_ANALYSIS.md`) is not supported by actually exercising the code.

### 3.1 SQL Injection — pre-authentication, exploitable via plain GET
`check_user_credentials()` (`main.cpp:369-374`) and `Authentication::authenticate()` (`authentication.cpp:21-24`), `getUserById`, `getUserByUsername`, `hasRole`, `verifyPassword`, `updateLastLogin` (`authentication.cpp`, all methods) build SQL by raw string concatenation of the `username`/`password` request parameters — **no escaping, no parameterized queries** (`postgresbc.cpp` has no `PQescapeLiteral`/`PQexecParams` anywhere). This function gates *every single command* (`main.cpp:2407-2408`), so it's the front door: a crafted `username` or `password` query-string value can attempt classic SQLi against the auth query itself. A partial, inconsistent sanitizer (`clean_value()`, `main.cpp:149-156`, strips `,"'()`) is applied to *some* other fields (product description, supplier, invoice details) but not to `barcode`, `rs_price`, `username`, `password`, or several others — so the mitigation is incomplete even where present.

### 3.2 Authorization is fake — every logged-in user is treated as admin
~~`Authentication::authenticate()` hardcodes `result["role"] = "admin"` for **any** successful login regardless of the DB row's actual role (`authentication.cpp:47`, marked `// TODO: Parse from JSON response`). `Authentication::hasRole()` runs a `COUNT(*)` query, **discards the result**, and unconditionally `return true;` (`authentication.cpp:112-124`) — role checks always pass. Command dispatch in `main.cpp` only checks `is_active` (`main.cpp:2410-2414`), never role — so any active credential (staff, till, or the web store's own service account, §3.3) can call `adduser`, `addsupplier`, `revenuereport`, `taxsummary`, `process_refund`, etc. There is no staff/admin privilege separation despite it being listed as a completed feature ("Role-Based Access (staff levels)") in `MISSING_FEATURES_ANALYSIS.md`.~~ — **role is parsed and checked correctly since §0.1/§7.4; the dispatch gate was binary (any-privileged vs. not) until §9 split it into three real tiers.**

### 3.3 Hardcoded service credentials shipped in public JS, pointed at a live demo server
The web store hardcodes `username: 'web_store_user', password: 'web_store_pass'` directly in client-side source, used for `validatecart` (`cart.js:126-127`), `webstoreorder` (`cart.js:296-297`), `orderhistory` (`order-history.html:371`), `getreceipt` (`receipt.html:379`), and **`salesreport`/`revenuereport`/`inventoryreport`/`taxsummary`-capable `reports.html:466`** — all sent to a hardcoded `https://demo.pgbc.ai/cgi-bin/boudica_pos` (`epos.js:7`, `cart.js:132`, `order-history.html:376`, `reports.html:472`). Anyone who views-source on the public storefront gets a working credential that, per §3.2, has no role restriction — meaning a member of the public can pull full financial reports (revenue, margins, VAT) and potentially call admin commands, and every unconfigured deployment of this open-source project silently sends customer traffic to the vendor's own demo instance until someone edits the source.

### 3.4 Card payments on the till never complete the sale
`handlePayment()` in `till.js:425-447` opens the Stripe card modal and **returns immediately** — `updateStockForTransaction` (stock decrement/`sellitem`), receipt printing, and `clearSale()` are only reached for cash/other payment methods. `till-card-payment.js`'s success handler never calls back into `till.js`. Net effect: a successful card charge on the till doesn't decrement stock, doesn't print a receipt, and doesn't clear the till for the next sale. Also both `till-card-payment.js:7` and `stripe-checkout.js:7` ship a hardcoded placeholder `STRIPE_PUBLISHABLE_KEY` (`pk_test_...`) with a `// TODO: Set to actual key from config` — Stripe is non-functional until someone edits source, there's no config-driven key injection.

### 3.5 No Stripe webhook endpoint despite webhook-verification code existing
`stripe.cpp:205-246` implements signature verification for incoming Stripe webhooks, but grep confirms **no command in `main.cpp` ever calls it** — there's no webhook receiver at all. Payment state only updates when the browser tab stays open long enough to call `confirm_payment` client-side; a closed tab, failed redirect after 3-D Secure, or a refund/dispute actioned from the Stripe dashboard will never reach the store's database. This is a materially different (and more specific) problem than the "payment reconciliation dashboard" bullet in `MISSING_FEATURES_ANALYSIS.md`.

### 3.6 Credentials-in-URL and plaintext localStorage password, everywhere
Every API call across both the till (`till.js`, `login.js`, `dashboard.js`, `stock_take.js`, `add_stock_item.js`, `show_products.js`) and web store sends `username`/`password` as **plaintext GET query-string parameters** — logged in web server access logs, browser history, and any intermediate proxy. The till additionally stores the password in plaintext `localStorage` (`login.js:2-3, 62-63`) and reads it back for every request.

### 3.7 Stored-XSS chain
~~Product/order data is rendered via unescaped `innerHTML` template literals in multiple places (`show_products.js:42`, `app.js:46,89`), and the AI advice response is explicitly rendered raw: `messageElement.innerHTML = htmlContent; // response is expected to be formatted HTML from the API` (`app.js:118-119`). Because `addproduct`/`addsupplier` accept attacker-reachable text (§3.3 gives an anonymous caller a valid credential to call these), and because the till stores the password in `localStorage` (§3.6), a crafted product description or a prompt-injected AI response can chain into full credential theft from a staff member's till session.~~ — **till-side fixed, see §9** (`app.js` doesn't exist in `code/web/` — that citation was either the web store's `code/web_store/js/app.js`, out of scope here, or stale from an earlier codebase state; either way no till file named `app.js` renders an AI response, and no till page currently has a `getadvice`/`stockanalysis`/`salesanalysis` UI to render one). The credential-theft chain itself is also narrower now than described: the token (not a password) is what `localStorage` holds since §8, and it's a 12-hour-lived, revocable session token rather than the account's actual password.

### 3.8 CORS wide open + disabled IP allowlist
~~Every response sends `Access-Control-Allow-Origin: *` (`main.cpp:2177`). The `server_whitelist` IP-allowlist check has its result unconditionally overridden right after being computed: `is_white_listed = true;` sits on its own line immediately before the `#endif` regardless of the check above it (`main.cpp:2281`), so the allowlist is dead code.~~ — **CORS wildcard fixed, see §8** (the dead `server_whitelist`/`is_white_listed` allowlist code itself was already removed entirely in §0.1's auth fix, not just overridden).

### 3.9 Email is fully simulated
`sendEmailReceipt`-equivalent in `cart.js:245-277` literally does `console.log("--- SIMULATING EMAIL RECEIPT ---")` — there is no email transport anywhere in the codebase (matches what the existing docs claim as "missing", confirmed accurate here).

### 3.10 Remote command execution via `getadvice` (shell injection, pre-auth given §3.1)
`getadvice` (`main.cpp:2791-2804`) takes the raw `prompt` parameter (only URL-decoded) into `get_advice()` (`main.cpp:1527-1535`) → `call_boudica()` (`main.cpp:44-68`), which builds a shell string `"curl ... -d '" + payload + "'"` (`main.cpp:64`) and runs it via `popen()` (`Utils::exec()`, `utils.cpp:335-340`). A single quote in `prompt` breaks out of the shell string — arbitrary command execution as the CGI process user. Combined with the auth bypass in §3.2/§3.8, this is reachable with no valid credentials at all.

### 3.11 Payments are never persisted — `payment_transactions` is a phantom table
None of `initiate_payment`, `confirm_payment`, `till_card_sale`, or `process_refund` write to the database (`main.cpp:2960-3141` — no `runCommand`/insert in any of these blocks), despite `store.payment_transactions` (`setup.sql:183`) existing with full Stripe fields for exactly this purpose. `till_card_sale` generates its `till_trans_id` with `rand()` (`main.cpp:3086-3105`) and returns it without saving it anywhere — the ID is unrecoverable the moment the response is sent. This is a different, more severe problem than "no reconciliation dashboard" (as the older docs frame it): the system has no record of card payments at all beyond what Stripe itself retains.

### 3.12 No server-side price recomputation — client-supplied total is trusted
`initiate_payment`/`till_card_sale` take `total`/`total_value` straight from the request (`std::stod(total_value)`, `main.cpp:3001/3091`) and create a Stripe PaymentIntent for that amount with no recomputation from the actual cart/item prices server-side. A caller (including the future external POS integration) can request a PaymentIntent for any amount it likes, decoupled from what's actually in the order.

### 3.13 Hardcoded near-production credentials in public storefront JS, feeding a raw-SQL executor
`code/web_store/js/data.js:10-11` hardcodes `creds.User('sibain@omniindex.io')` / `creds.Password('Ch35t3rP455w0rd')` (comment: "Only for testing on an none whitelist box") and uses them against `driver/pgbc.js`, a generic string-interpolated-SQL-over-HTTP executor hitting `/cgi-bin/pgbcadmin` — a different, more powerful admin endpoint than the POS API itself. If that account has real database privileges, this is a full database compromise reachable by anyone who views-source on the storefront.

### 3.14 Schema-level defects
- `code/sql/setup.sql:2` hardcodes a plaintext database password (`Th3Cur1051tyP455word`) in a source file destined for an MIT-licensed public repo.
- `period_sales` (`setup.sql:109-121`) declares indexes with inline `INDEX idx_...(...)` clauses inside `CREATE TABLE` — this is MySQL syntax; it is not valid PostgreSQL DDL and will fail outright when this schema file is run against Postgres (the same file elsewhere uses `CREATE EXTENSION vector`, confirming Postgres is the actual target).
- `search_functions.sql` defines six real, usable functions — `search_products_fulltext`, `search_products_semantic`, `search_products`, `search_by_barcode`, `search_by_supplier`, `get_low_stock(threshold)` — none of which `main.cpp` ever calls. The touted "full-text/semantic search" and a ready-made low-stock-alert query both exist in the database layer and are wired to nothing in the API.
- `reward_points` (loyalty) and `workshops` beyond read-only listing are similarly present in schema with zero write-path code, confirming §4's "table only" status independently.

### 3.15 Outbound TLS verification disabled + another hardcoded API key
~~The geo-IP lookup in `http.cpp:55` hardcodes a RapidAPI key in source, and the same call disables certificate validation entirely — `CURLOPT_SSL_VERIFYPEER, 0L` / `CURLOPT_SSL_VERIFYHOST, 0L` (`http.cpp:58-59`) — a man-in-the-middle exposure on that outbound request.~~ — **fixed, see §9.** Also found and fixed the same disabled-verification pattern in the neighboring, equally-unused `Http::request()` (`http.cpp:122-123` in the original line numbering), not previously called out separately.

### 3.16b `getdetails` is broken against the shipped schema (found by actually running it)
`get_details()` (`main.cpp:1085` and `main.cpp:1117`) orders its product-lookup queries by `p.modified_date` / `modified_date`, a column that does not exist anywhere in `code/sql/setup.sql` — `store.products` only has `updated_at`. Every `getdetails` call (product lookup/search, used by both the till's Lookup tab and the web store's search box) fails outright with a Postgres error. This wasn't visible from static reading of `main.cpp` alone — it only surfaced once the schema was actually instantiated and exercised end to end. Patched at container-build time (`docker/backend/Dockerfile`) by rewriting the column name; the tracked source is left as-is.

### 3.16 POST body silently truncated at 4095 bytes
The CGI POST body reader uses a fixed buffer and never checks `Content-Length` for overflow (`main.cpp:2248-2250`). A large cart/order payload (many line items, as a busy till or a bulk API-driven order from the planned POS integration would send) is silently truncated rather than rejected — the request will parse whatever partial JSON/query-string survives the cut, which is a correctness landmine, not just a size limit.

---

## 4. Feature Gap Analysis vs. Comparable POS Systems (Square, Shopify POS, Lightspeed, Clover, Toast)

The earlier `MISSING_FEATURES_ANALYSIS.md`/`README.md` gap lists are directionally correct on breadth but were written without checking the code, so severity/priority is recalibrated here against what a real competing EPOS needs, and I've folded in the security items above since those are launch-blockers ahead of feature parity.

| Area | Status | Notes |
|---|---|---|
| **Auth/RBAC** | 🔴 Broken, not just missing | §3.1–3.3. Fix before anything else — this is worse than "no role management," it's "role management that lies." |
| **Payment reconciliation** | 🔴 Broken | §3.4, §3.5. Till card sales silently fail to reconcile; no webhook safety net. |
| **Discounts/promotions** | ❌ Absent | No table, no command, no UI. Table stakes for every competitor listed above. |
| **Returns/refunds workflow** | ⚠️ Partial | `process_refund` exists (Stripe-only refund, `main.cpp:3111`), but no reason codes, RMA, restocking-to-shelf logic, or non-Stripe (cash) refund workflow beyond negative-quantity till entries. |
| **Loyalty program** | ❌ Effectively dead despite one write path | `add_user()` inserts a single zeroed `store.reward_points` row per new user (`main.cpp:1517`, correcting an earlier pass that called this table fully orphaned) — but nothing ever increments `points_earned` on a sale, nothing reads/redeems points, and no frontend surface displays them. A stub insert, not a loyalty program. |
| **Multi-location** | ❌ Absent | Schema and commands assume one store throughout (no `location_id` anywhere). |
| **Inventory reordering** | ❌ Absent (forecasting ≠ reordering) | `predict_reorder_date` gives a recommendation number; there's no PO generation, no supplier email, no min/max thresholds enforced automatically. |
| **Email/SMS notifications** | ❌ Absent, confirmed simulated | §3.9. No SMTP/SendGrid/Twilio integration exists. |
| **Barcode label printing** | ❌ Absent | Till can *scan* and *print receipts* but has no label/price-tag printing path. |
| **Staff scheduling/shifts/commission** | ❌ Absent | `adduser` only; no shift, clock-in, or commission tables/commands. |
| **Accounting export** | ⚠️ CSV only | No QuickBooks/Xero API integration; reports are queryable but not synced. |
| **2FA** | ❌ Absent | Single-factor password only, and that auth path is SQLi-vulnerable (§3.1). |
| **Gift cards / subscriptions / bundles** | ❌ Absent | No schema support. |
| **Offline mode** | ❌ Absent | Till has no service worker/offline queue; a lost network connection mid-sale has no documented fallback (cash sale flow requires live fetch to `sellitem`). |
| **Multi-currency** | ❌ Absent, GBP hardcoded | VAT logic (`TAX_RATE`) and `£` formatting hardcoded throughout frontend. |
| **Workshop/event booking** | ⚠️ Read-only | `getworkshops` lists upcoming events (`main.cpp:552-613`); no registration, capacity enforcement, or payment-linked booking. |
| **Payment persistence/reconciliation** | 🔴 Broken | §3.11, §3.12, §3.5. No DB record of any card payment; no server-side amount recomputation; no webhook safety net. |
| **Search (full-text/semantic)** | ❌ Built, unwired | §3.14. Six working SQL functions exist and are never called from the API. |

---

## 5. Implications specifically for the planned POS-as-external-API-caller integration

The new POS system is meant to call this backend as a pure API client — no shared session, no shared codebase. That framing makes several of the above worse than they'd be for an internal integration:

- **There is no real API authentication scheme to integrate against.** The existing model is "resend a username/password as a GET query-string param on every call," gated by an auth check that's SQL-injectable (§3.1) and bypassable by omitting credentials entirely (§3.8), behind a role system that always reports back `admin` (§3.2). An external POS should not be pointed at this as-is; it needs a real API-key/token layer added first, or the POS becomes the easiest path to fully compromising the backend.
- **Financial integrity matters more from an external caller.** §3.11 (payments not persisted) and §3.12 (client-supplied total trusted) mean a POS integration could not currently produce an auditable, tamper-resistant record of what it charged — every payment's source of truth would live only in Stripe, not in Boudica's own database.
- **The 4095-byte POST truncation (§3.16)** will bite a POS sending structured multi-item orders/carts as JSON, in a way it wouldn't for the existing till (which sells mostly one item at a time via query params).
- **CORS being `Access-Control-Allow-Origin: *`** (§3.8) is actually *convenient* for a cross-origin API caller with no cookies involved — but it should be paired with real per-request authentication, which is currently missing, not with the fake whitelist that exists today.

## 6. Recommended Priority Order

1. ~~**Fix authentication/authorization before anything else** — parameterize all SQL (switch to `PQexecParams`), make `hasRole()` actually check the query result, stop hardcoding `role = "admin"` on every login, add per-command role gating in the `main.cpp` dispatch, and remove the unconditional `is_white_listed = true` override.~~ — **done, see §0.1/§7.4.**
2. ~~**Design a real API-key/token scheme for the planned POS integration** rather than reusing the credentials-in-query-string model — this is a new external caller, not more internal code, and needs its own auth boundary.~~ — **session-token scheme done for the till, see §8. The web store hasn't migrated (still resends username/password, which the backend still accepts — see §8's closing note) — worth revisiting once a real external POS caller is actually being integrated.**
3. **Remove hardcoded credentials** from the storefront source (`web_store_user`/`web_store_pass` in `cart.js` etc., and the `sibain@omniindex.io`/`Ch35t3rP455w0rd` pair in `data.js`) **and the demo-server URL**; make backend URL and any service credentials deployment-time config, not committed literals. Rotate any of these that may have been real.
4. **Close the RCE in `getadvice`** (§3.10) by calling the Gemini/Boudica HTTP API directly via libcurl instead of shelling out through `popen()`.
5. **Make payments durable**: write `payment_transactions` rows on `initiate_payment`/`confirm_payment`/`till_card_sale`/`process_refund`, recompute totals server-side from cart contents, and wire the existing webhook-signature verifier to an actual endpoint.
6. **Fix the till card-payment flow** to actually finish the sale (stock, receipt, clear).
7. ~~**Stop sending credentials in URLs**; move to POST + a real session/token scheme instead of resending username/password on every call, and stop storing the password itself in `localStorage`.~~ — **done for the till, see §8** (POST move was §7.6; the token scheme and no-longer-storing-the-password part is §8). Web store unchanged.
8. **Escape/encode all `innerHTML` insertions** of backend-sourced data (product descriptions, AI responses) to close the stored-XSS chain.
9. **Fix the schema bugs** (§3.14): the MySQL-syntax `INDEX` clause in `period_sales` that breaks on Postgres, and either wire up or remove the six orphaned search functions.
10. Then tackle the feature gaps in §4, roughly in the order: discounts → returns workflow completion → reordering/PO generation → email notifications → loyalty → multi-location.

---

## 7. Till-Only Audit (2026-09-09)

Scoped explicitly to the till dashboard (`code/web/`) and the backend commands it actually calls — the web storefront (`code/web_store/`) and its customer-facing commands are out of scope here. Per the user: **this is a single-store system**, so multi-location/multi-store gaps are deliberately not listed as findings below. Produced by two parallel forked passes (one frontend-only, one backend-only, cross-checked against each other for orphaned API surface) plus direct live re-verification of the two highest-severity items against the running Docker stack — same methodology as §0/§0.4.

### 7.1 Broken — exists, but doesn't actually work (or is actively dangerous)

Roughly ranked by how much damage a real till operator would hit:

1. **`sellitem` crashes the whole CGI process on a non-numeric `price` or empty `quantity`.** `update_sale()` (`main.cpp`) uses raw `std::stol`/`std::stod` on caller input instead of the `safe_stol`/`safe_stod` helpers already used elsewhere (§0.2 explicitly flagged this exact gap as unswept — this is it). **Live-confirmed** (2026-09-09): `price=notanumber` or `quantity=` returns an empty HTTP 200 body; Apache's error log shows `terminate called after throwing an instance of 'std::invalid_argument' / what(): stol`. This is the core sale-recording path — any scanner glitch, malformed request, or operator typo that reaches this parameter unvalidated kills the sale with no error message at all.
2. **`updatestock` has the identical crash** (`update_stock()`, `main.cpp`, `std::stol(delta)`). **Live-confirmed** the same way — `quantity=notanumber` produces the same empty-body/`terminate called` signature. This is the manual stock-adjustment path (`stock_in.js`/`add_stock_item.js`).
3. **Barcode scanning at the till cannot add a priced item to a sale.** `till.js`'s `handleBarCode()` truncates the `getdetails` JSON response at the first `}` it finds — landing inside the nested item object, before the response actually closes — so `JSON.parse` throws, caught by an empty `catch{}` that swallows it silently. Even were that fixed, it reads `json.description`/`json.rs_price` off the wrong (top-level, non-nested) object with the wrong field name (`price`, not `rs_price`). Net effect: scanning a barcode does nothing, silently. The identical bug in `add_stock_item.js`'s duplicate-barcode check means duplicate-barcode detection there can never trigger either.
4. **Card payments never complete the sale.** `till.js`'s `handlePayment()` opens the card-payment modal on "Card" and returns — nothing runs afterward. `till-card-payment.js`'s success handler charges Stripe and closes the modal, but never calls `sellitem`, never prints a receipt, never clears the cart. A successful card charge leaves stock/sales unrecorded and the same items still sitting in the till.
5. **Card payments can't function regardless of (4)** — `till-card-payment.js` ships `const STRIPE_PUBLISHABLE_KEY = 'pk_test_1234567890'; // TODO: Set to actual key from config`, a placeholder value, not a real key.
6. **The till has no way to know whether a sale actually recorded.** The backend CGI always returns HTTP 200 regardless of business-logic outcome — `{"error": "..."}` is still a 200. `till.js` only checks `response.ok` (always true) and never inspects the JSON body, so the receipt prints and the till clears the sale even when `sellitem` failed server-side (including via findings 1/2 above). Combined with (1)/(2), a crash is not just silent on the backend — the frontend actively tells the operator it worked.
7. **`addproduct` creates duplicate products instead of replacing/rejecting.** It looks up an existing row by `(supplier, description)` and, if found, deletes by the *new incoming* barcode rather than the barcode it just found (`main.cpp` — wrong variable in the `DELETE`). Adding a product with the same supplier+description but a different barcode leaves two rows for the same item.
8. **No permission enforcement anywhere in the backend dispatch — `role` is stored and returned correctly but never checked.** `Authentication::hasRole()` works correctly (fixed in §0.1) but is never called from `main.cpp`. **Live-confirmed**: the seeded `operator`-role `testuser` account successfully called `revenuereport`, `addsupplier`, and `adduser` — none of which a non-admin till operator should be able to reach. The dispatch chain gates only on `is_active`, never on `role`.
9. **`till_card_sale` unconditionally reports `"success": true`** regardless of what Stripe actually returned, with no check that `stripe_key` is even configured. Combined with the pre-existing finding that no payment command (`initiate_payment`/`confirm_payment`/`till_card_sale`/`process_refund`) ever writes a `payment_transactions` row (§3.11, reconfirmed still true), a till card sale can report success with zero record anywhere in Boudica's own database of what was charged.
10. **`process_refund` likely sends the wrong Stripe ID type.** `refundPayment()` (`stripe.cpp`) always sends `charge=<id>`, but the command's own parameter is `payment_intent_id` — a PaymentIntent ID (`pi_...`) is not a valid Charge ID (`ch_...`) for Stripe's refunds endpoint, which wants `payment_intent=` for that shape. Not live-verified (no Stripe key configured in this environment) — flagged from reading the code, needs confirming against a real Stripe sandbox.
11. **Three of the four AI sales forecasts are broken.** `predict_daily_sales`/`predict_weekly_sales`/`predict_monthly_sales` all build a pure-aggregate `SELECT SUM/COUNT/AVG ... ORDER BY sale_date` with no `GROUP BY` — invalid SQL, Postgres rejects every call (`column "period_sales.sale_date" must appear in the GROUP BY clause...`). Only `predict_reorder_date` (a genuinely different, correctly-grouped query) works. Live-confirmed for all three.
12. **Stocktake counts can leak across sessions.** `stock_take()` inserts a row into `store.stock_take` on every scan; the only place that ever clears a barcode's rows is `update_stock()`'s delete, which the UI only triggers for barcodes with an actual discrepancy (`complete_stock_take()` explicitly skips `difference == "0"` items). A barcode that counts correctly is never cleared, and the next stocktake session's `complete_stock_take()` query (`GROUP BY barcode`, no date/session scoping) aggregates *every row ever inserted*, silently inflating future counts. Read-verified; not live-reproduced (needs a two-session repro).
13. **The dashboard's "Sales Comparison" chart is entirely fabricated.** `dashboard.js` hardcodes `last24h`/`previous24h` figures behind a fake `setTimeout` labeled "Simulate network delay" — no backend call at all, and nothing in the UI marks it as placeholder data. It looks like a real live comparison.
14. **Credentials sent as a GET query-string parameter on nearly every till API call, and the password stored in plaintext in `localStorage` indefinitely** (`login.js` stores it; essentially every other script resends it in the URL). Already flagged system-wide in §3.6 — reconfirmed current and specifically affecting every till screen, not just login.
15. **`DEBUG = true` is shipped**, with the developer's own comment reading `/* MUST BE SET TO FALSE ON IMPLEMENTATION */` (`application.js`). Every API response gets logged to the browser console in production as a result.

### 7.2 Missing — no implementation, or fully built-but-never-wired

1. **No stock-movement audit trail, despite the backend already supporting one.** `move_stock_in()`/`move_stock_out()` (`main.cpp`) are transaction-safe, upsert-correct, and record a `reason` + `supplier` against every stock change in `store.stock_removal` — but **no dispatch command calls either one**. The till only ever calls the generic `updatestock`, which has no `reason` field at all. A till operator currently has no way to record *why* stock changed (breakage, shrinkage, supplier return, count correction) even though the schema and backend logic already fully support it — this just needs two dispatch cases and a small frontend change, not new backend work.
2. **Special Orders are never saved anywhere.** The form validates, prints a paper receipt via the Star printer, and stops — the code's own comment says *"Optionally, send data to a server here. For now, we just print and clear."* No backend call, no DB table, no dispatch case. If the paper receipt is lost, the order is gone; there's no way to look it up, track fulfillment, or reconcile a deposit later.
3. **Invoice attachments are silently discarded.** `invoice_in.js` builds a real drag-and-drop upload UI and POSTs the file as multipart form data, but `addinvoice`'s dispatch case never reads multipart bodies — `processUpload()`/`parseMultipartData()` exist in `main.cpp` but are never called from anywhere, and `store.suppliers_invoices` has no file/attachment column. The uploaded file goes nowhere; the invoice record is saved without it.
4. **Payment reconciliation.** Following from 7.1's findings 9/10: since no payment command persists anything, there is no way to reconcile "what Boudica thinks it sold today" against "what Stripe actually charged" without manually cross-referencing Stripe's own dashboard.
5. **Role-based access control enforcement** (the flip side of 7.1 finding 8) — the data model (`role` column, `hasRole()`) is already there; it's purely a missing wiring step in the dispatch chain, not a schema/design gap.

### 7.3 Nice-to-have

1. ~~Printer/cash-drawer failures are silently swallowed — `printer.js`'s error-handling `alert()` calls are commented out, and the cash-drawer-open call has an empty `catch{}`. An operator gets no feedback if a receipt or the drawer actually fails to open.~~ — **fixed, see §10.**
2. ~~The Star WebPRNT printer URL is hardcoded to `http://localhost:8001/...` with the code's own comment noting it should be configurable.~~ — **fixed, see §10.**
3. ~~Receipts hardcode "Curiosity Cabin" / "www.thecuriositycabins.com" branding — leftover from a specific prior deployment, not configurable for a fresh install of this template.~~ — **fixed, see §10.**
4. ~~`forecasting.js` hardcodes an absolute `http://localhost/cgi-bin/boudica_pos` URL, inconsistent with every other script's relative path — would break if the till is ever reached via anything other than literally the hostname `localhost`.~~ — **fixed in §8's `apiCall()` migration** (already closed before this list was re-checked; confirmed still closed).
5. No offline/retry handling anywhere — any fetch failure just shows a toast and drops the operation; nothing queues or retries over a flaky LAN connection (a real consideration for a physical till on in-store Wi-Fi). **Not attempted — genuinely bigger scope than the rest of this list (a retry/queue layer, not a one-file fix).**
6. Chart.js and Stripe.js load from external CDNs with no local fallback — a till with no internet loses dashboard charting and the ability to take card payments (core cash-sale recording stays fine, being local-network-only). **Not attempted this pass.**
7. ~~Dead code worth removing: an entire second, unreferenced card-payment entry point in `till-card-payment.js` (`addCardPaymentOption`/`handleCardPaymentClick`, wired to a `localStorage` key nothing ever sets); several vendored-but-unloaded Star SDK files and two very old (2011/2012) jQuery builds sitting in `scripts/star/` that nothing in `index.html` actually `<script src>`s.~~ — **fixed, see §10.**
8. ~~`get_dashboard`'s empty-dataset placeholder still produces a stray `""`-keyed entry in the day-by-day breakdown JSON on a fresh/quiet dataset~~ — **fixed, see §7.7** (turned up already resolved in the working tree by the time of the §7.7 pass, as a side effect of the 7.1#13 dashboard-chart rebuild; not separately documented until now).

### 7.4 Backend fixes applied this session (2026-09-09, continued)

Working through §7.1's backend-fixable findings in order. Each rebuilt and re-tested against the running container after the change (crash fixes reproduced live before the fix, then reconfirmed non-crashing after).

- **7.1#1/#2 — `sellitem`/`updatestock` crash on non-numeric input.** `update_sale()`/`update_stock()` now use the existing `safe_stol`/`safe_stod` helpers instead of raw `std::stol`/`std::stod` on caller-supplied `price`/`quantity`. Live-reconfirmed non-crashing after.
- **7.1#7 — `addproduct` duplicate-row bug.** The replace-existing-product `DELETE` now targets the existing row's own barcode (`json_str(jSupplier, "barcode")`) instead of the new incoming barcode.
- **7.1#8 — no permission enforcement in dispatch.** Added `user_is_privileged()` (role `admin`/`manager`) and gated `addproduct`, `addsupplier`, `addinvoice`, `adduser`, `salesreport`, `revenuereport`, `inventoryreport`, `taxsummary`, and `process_refund` behind it. Every other till command (selling, stock adjustments, cashup, dashboards, lookups, forecasts) stays reachable by any active user, matching current till usage. **This single binary tier was later split into three real ones — see §9.**
- **7.1#9 (partial) — `till_card_sale` unconditional success.** Now parses Stripe's actual response and reports failure if it errored or has no `id`, instead of always returning `"success": true`.
- **7.1#10 — `process_refund` wrong Stripe ID type.** `refundPayment()` (`stripe.cpp`) now sends `payment_intent=` instead of `charge=`, matching the PaymentIntent ID (`pi_...`) every caller actually passes. Not live-verified against a real Stripe sandbox (no `stripe_secret_key` configured in this environment) — SQL/logic verified as below; the Stripe-side field name is a read of Stripe's own refunds API docs, flagged for confirming against a real key.
- **7.1#11 — three broken AI sales forecasts.** `forecast_daily_sales()`/`forecast_weekly_sales()`/`forecast_monthly_sales()` had an invalid `ORDER BY sale_date` alongside aggregates/`GROUP BY` that don't include it. Daily forecast's `ORDER BY`/`LIMIT` removed entirely (the query already collapses to one summary row); weekly/monthly now order by the grouped expression itself.
- **7.1#9 (the rest) / §3.11 / §7.2#4 — payments were never persisted.** `store.payment_transactions` existed with full Stripe fields but nothing wrote to it. Now:
  - `initiate_payment` inserts a row (`payment_status` = whatever Stripe returns for a freshly-created, unconfirmed intent, e.g. `requires_payment_method`) once Stripe confirms the intent was actually created — checked via the same "has `id`, no `error`" pattern already used for `till_card_sale`. `order_id` is set from the caller's order — confirmed by reading `code/web_store/js/cart.js`/`stripe-checkout.js` that the web store always calls `webstoreorder` (which inserts into `store.customer_orders`) *before* opening the payment modal, so the row `payment_transactions.order_id` references via FK already exists by the time this INSERT runs.
  - `confirm_payment` re-queries Stripe directly for the PaymentIntent's real status (not just trusting whatever the browser claims) and `UPDATE`s the same row by `stripe_payment_intent_id`, also picking up `stripe_charge_id` from Stripe's `latest_charge` field when present (`COALESCE`d against the existing value so a status-only re-check never blanks out a charge ID recorded earlier).
  - `till_card_sale` inserts a row with `order_id` left `NULL` (till sales have no `store.customer_orders` row to reference) and `till_transaction_id` set to the same ID previously generated and returned but never saved anywhere — this closes the exact gap flagged in 7.1#9 ("the ID is unrecoverable the moment the response is sent").
  - `process_refund` `UPDATE`s the row's `refund_status`/`refund_amount_cents`/`refund_reason`/`refunded_at` and flips `payment_status` to `refunded`, read from Stripe's actual refund response rather than assumed.
  - All four use `execParams`/parameterized queries, consistent with the rest of the codebase post-§0.1.
  - **Verified**: all four SQL statements (insert/upsert/update, including the `NULLIF(..., '')::numeric` empty-string-to-NULL path for `refund_amount_cents`) run cleanly against the real `store.payment_transactions`/`store.customer_orders` schema in a rolled-back transaction in the live container DB. The backend rebuilds cleanly and the three commands reachable without a configured Stripe key (`initiate_payment`, `till_card_sale`, `process_refund`) all correctly short-circuit with a clean JSON error and no crash. **Not live-verified against a real Stripe test key** — this environment has no `stripe_secret_key` configured (`docker/backend/boudica_pos.conf.template` renders it empty) — so the actual Stripe response shapes (`id`, `status`, `latest_charge`, refund `amount`) are as documented by Stripe's API, not confirmed against a live charge. Flagged for whoever has a Stripe test key to run one real card-present and one real refund through the till/web-store flow end-to-end.

### 7.5 Remaining §7.1/§7.2 backend items fixed (2026-09-09, continued)

- **7.1#12 — stocktake counts leaking across sessions.** `complete_stock_take()` now deletes every barcode's `store.stock_take` rows as it processes them, regardless of whether a discrepancy was found — previously only `update_stock()` cleared rows, and only for barcodes the UI flagged as discrepant, so a barcode that counted correctly was never cleared and inflated every future session's count. **Live-verified** with a throwaway test product: a 2-scan session against a stock quantity of 2 (no discrepancy) now leaves zero `stock_take` rows behind afterward (previously they'd persist indefinitely); a following 1-scan session then correctly reports `counted: "1"` against the real stock of 2 — not the leaked `3` it would have shown pre-fix.
- **§7.2#3 — invoice attachments silently discarded.** `addinvoice`'s multipart form (`code/web/scripts/invoice_in.js`) always posted the file correctly; nothing on the backend ever read a multipart body. Fixed:
  - Added `MultipartField`/`parseMultipartFields()` (`main.cpp`), which walks the parts `parseMultipartData()` already split out (previously unused/dead) and reads each part's `Content-Disposition` `name=`/`filename=`. main()'s POST-body handling now detects `Content-Type: multipart/form-data` and rewrites the parsed fields into an ordinary percent-encoded `key=value&...` string (new `url_encode()` helper) so `QueryData` and every handler's existing `url_decode()` call keep working unchanged; the file part is pulled out separately into `uploaded_invoice_filename`/`uploaded_invoice_content`.
  - **Also required fixing §3.16 (POST body silently truncated at 4095 bytes)** as a dependency — a real file attachment almost always exceeds the old fixed 4096-byte stack buffer, which never checked `Content-Length` before truncating. Replaced with a heap buffer sized to the real `Content-Length`, capped at `MAX_FILE_SIZE`.
  - `processUpload()` (previously dead code, never called from anywhere) now actually enforces the pre-existing but also-unused `ALLOWED_EXTENSIONS` allowlist (case-insensitive) before writing anything to disk, since wiring this up makes arbitrary uploaded content genuinely reachable for the first time. Returns the sanitized, timestamp-prefixed filename actually written (instead of `bool`) so two invoices attaching a same-named file don't clobber each other.
  - Added `store.suppliers_invoices.attachment_filename` (tracked in `setup.sql` + applied live via `ALTER TABLE`) and threaded it through `add_invoice()`.
  - **Found and fixed only by running it end-to-end**: `UPLOAD_DIR` was a path relative to the CGI process's own working directory (`"uploads/"`), which mod_cgid sets to `/usr/lib/cgi-bin/` — owned by `root`, not writable by `www-data` (the account Apache's CGI workers actually run as). Every write silently failed (`mkdir`/`ofstream` both hit `EACCES`/`ENOENT`) the first time a real upload was attempted, caught by `processUpload()`'s own try/catch and reported back as a generic rejection with no indication it was a filesystem-permissions problem, not a validation failure. Fixed by moving `UPLOAD_DIR` to an absolute `/var/lib/boudica_pos/uploads/`, created and `chown`'d to `www-data` by `docker/backend/entrypoint.sh` at container start.
  - **Verified live end-to-end**: a real multipart upload (`curl -F`) is saved to disk under the new path with correct ownership, the DB row records the resulting filename, a disallowed extension (`.exe`) is cleanly rejected before anything touches disk, and `addinvoice` without any attachment still works unchanged. Also reconfirmed an ordinary urlencoded POST body still parses correctly after the buffer rewrite.
- **§7.2#2 — Special Orders never saved.** Previously print-only (`code/web/scripts/special_orders.js`: *"Optionally, send data to a server here. For now, we just print and clear."*) with no table, command, or way to look an order up again if the paper receipt was lost. Added `store.special_orders` (`order_number`, `customer_name`, `customer_address`, `products`, `total_value`, `deposit_amount`, `due_date`, `status`, tracked in `setup.sql` + applied live), `add_special_order()`, and the `addspecialorder` dispatch command; `special_orders.js` now saves the order (client-generated `order_number`) before printing the receipt, and shows an error instead of printing/clearing if the save fails. **Verified live**: a real order round-trips with its free-text `products` field (containing commas and parentheses) intact — no `clean_value()` applied there, since it would otherwise mangle a normal product list and the query is already parameterized — and a duplicate `order_number` is cleanly rejected via the table's `UNIQUE` constraint. No listing/lookup UI was added (out of scope here — the audit finding was specifically that no record existed at all); the row is queryable directly for now.

All of the above rebuilt cleanly, migrations applied live (as the `postgres` owner, with `GRANT`s to the `store` app role — the `store` role isn't the table owner in this environment), and every test row created during verification was cleaned up afterward.

### 7.6 Remaining §7.1 frontend items fixed (2026-09-09, continued)

Working through the rest of §7.1 — the till-frontend-only findings deliberately left out of the §7.4/§7.5 pass. Rebuilt (both backend and frontend images) and re-tested via direct `curl` against the running container after each change; `docker exec`'d into the frontend image to confirm the built JS matched source. No browser was available this session (the Chrome extension was declined), so UI interaction itself is unverified — only the actual backend request/response contracts every script depends on.

- **7.1#3 — barcode scanning couldn't add a priced item.** `till.js`'s `handleBarCode()` and `add_stock_item.js`'s duplicate-barcode check both truncated `getdetails`'s response at the first `"}"`, landing inside the nested object and throwing (silently swallowed by an empty `catch{}`), and read `json.description`/`json.rs_price` off the wrong (never-reached) top level. Root-caused via a live `getdetails` call: the real response always wraps its result in `{"products_search_details": [...]}` — even for an exact single-barcode match — with the price under `"price"`, not `"rs_price"`. Both call sites now read `products_search_details[0]`. **Found only by testing against the live backend, not from reading the code**: a barcode that genuinely doesn't exist doesn't return an empty array or an `"error"` key — it returns a *one-item* array of empty strings (the same placeholder-row quirk as §7.3#8) — so "not found" is now detected by checking `product.barcode` is non-empty, not array truthiness; the first version of this fix would have made every scanned barcode in `add_stock_item.js` incorrectly report "already in the system."
- **7.1#4 / #14 (dependency) — card payments never completed the sale, and couldn't reach Stripe at all.** `till.js`'s `handlePayment()` opened the card modal and returned; `till-card-payment.js`'s success handler charged Stripe and closed the modal without ever recording the sale. Extracted the cash-path's completion logic into a shared `completeSale()` (`till.js`, exposed as `window.completeSale`) — stock update, receipt print, till clear — and the card success handler now calls it once Stripe confirms the charge. **Found only by tracing the actual request, not visible from the finding as originally scoped**: the modal's own `processTillCardSale()` called `initiate_payment`, a command hard-requiring an `order_id` FK into `store.customer_orders` (a row a till sale never has — confirmed live: `{"error":"order_id is required"}` on every attempt) and never sent a `password` field at all, which alone fails the auth check. Rewired to call `till_card_sale`, `main.cpp`'s actual purpose-built equivalent for a till sale (`operator_id`, no `order_id`) — which in turn was missing `client_secret` in its own response despite `stripe.cpp`'s `createPaymentIntent()` deliberately using `confirm=false` "for the client to confirm" (its own comment); without it Stripe.js has nothing to call `confirmCardPayment()` with, so the charge could never complete however the frontend called it. Fixed by forwarding `client_secret` through. Confirmed live that `till_card_sale` now authenticates and gets past the order_id/password errors, returning a clean `{"error":"Stripe payment failed: "}` (this environment has no `stripe_secret_key` configured, same as every other Stripe command since §7.4 — **not live-verified against a real Stripe test key**, flagged the same way as those).
- **7.1#5 — Stripe publishable key was a hardcoded placeholder.** `till-card-payment.js` shipped `pk_test_1234567890`. A publishable key is safe to expose client-side by design, but still needs to come from real per-deployment config, not source. Added `stripe_publishable_key` to `get_configuration()`/`boudica_pos.conf.template`/`entrypoint.sh`/`docker-compose.yml` (same pattern as `stripe_secret_key`), and a new `getpublicconfig` dispatch command (`main.cpp`) that returns just that one non-secret value. `till-card-payment.js` now fetches it lazily before initializing Stripe. **Verified live**: `getpublicconfig` returns `{"stripe_publishable_key":""}` in this environment (no key configured, same as the secret key) — `initializeTillStripe()` correctly logs a warning and returns `false` rather than initializing with a fake key.
- **7.1#6 — till printed the receipt and cleared the sale even when the backend's own update failed.** The backend CGI always answers HTTP 200 regardless of business-logic outcome, so `response.ok` alone never caught a failed `sellitem`/`updatestock` call (including an ordinary `{"error": ...}` response, or the crash bugs from §7.4#1/#2 before those were fixed). `removeFromStock()`/`restockItem()` (`till.js`) now inspect the parsed response body and throw on `.error`; `completeSale()` only prints the receipt and clears the till if the stock update genuinely succeeded — otherwise it shows an error and leaves the sale on the till for the operator to retry.
- **7.1#13 — the dashboard's "Sales Comparison" chart was entirely fabricated.** `dashboard.js` hardcoded `last24h`/`previous24h` figures behind a `setTimeout` labeled "Simulate network delay," with nothing marking it as placeholder. Rebuilt on the same real `getdashboard` data the other two dashboard buttons already use: "today" from the live running total (`store.period_sales`), "previous" from the most recent day with a completed cashup (`store.cash_up`) before today, with an explicit "no prior day's cashup on record yet" message when there isn't one yet (a fresh install) instead of silently comparing against zero.
- **7.1#14 — credentials sent as a GET query-string parameter on nearly every till API call.** Added a shared `apiCall(command, params)` helper (`application.js`) used by every till script: sends credentials in a POST body instead of the URL (out of browser history and server/proxy access logs) and parses the *whole* JSON response instead of the truncate-at-first-`"}"` trick most call sites used, which is also the root cause behind 7.1#3/#6 above. Every ad hoc `fetch` across `till.js`, `add_stock_item.js`, `login.js`, `stock_in.js`, `product_lookup.js`, `special_orders.js`, `dashboard.js`, `order-history.js`, `reports.js`, `stock_take.js`, `eod.js`, and `till-card-payment.js` now goes through it. `invoice_in.js` (file upload) already POSTed credentials in a `FormData` body — only its truncation trick was removed. The password itself is still kept in `localStorage` indefinitely and resent on every call, same as before — genuinely removing that needs a real session-token mechanism on the backend, which is a bigger change than a frontend-only pass; flagged for a dedicated session, not attempted here.
- **7.1#15 — `DEBUG = true` was shipped.** Flipped to `false` in `application.js`.

**New findings surfaced while fixing the above (not fixed, logged for a future pass, per usual practice):**
- ~~`product_lookup.js`'s barcode-lookup handler doesn't have the same "empty placeholder row" problem 7.1#3 had...~~ — **fixed, see §7.7.**
- `till_card_sale`'s success path never calls `confirm_payment` afterward to reconcile the `payment_transactions` row against what Stripe actually confirmed client-side — it's created once at `till_card_sale` time with whatever status the initial (unconfirmed, `confirm=false`) PaymentIntent had, and nothing updates it once `stripe.confirmCardPayment()` succeeds. Extends §7.2#4 (payment reconciliation) rather than 7.1#4 itself (which is specifically about the till recording the *sale*, not the *payment status*) — flagged, not fixed, since it needs a real Stripe test key to verify either way.
- `login.js`'s login handler used to show the server's error toast on a failed login but then log the user in anyway (set localStorage, hide the login form) regardless — fixed in passing while touching that exact code path for 7.1#14's credentials change, since leaving it broken would have been actively worse than not touching it at all.

**Not attempted this pass**: a real Stripe test key to verify the card-payment chain (`till_card_sale` → Stripe → `completeSale`) charges an actual card end-to-end, and a browser to click through the till UI directly — both flagged above at the specific points they matter, consistent with how every other Stripe-dependent finding in this document has been handled since §7.4.

### 7.7 Three follow-up findings closed out (2026-09-10)

- **`product_lookup.js` blank-card bug (flagged in §7.6).** Same root cause as 7.1#3/§7.3#8: `getdetails` returns a *one-item* array of empty-string fields for a genuine "not found," not an empty array. The search handler's `products.length > 0` check was therefore always true for a real miss, rendering one product card with every field blank instead of the intended "No product found" message. Fixed the same way as till.js/add_stock_item.js: `products = json.products_search_details.filter(p => p.barcode)` before the length check. **Live-verified**: `getdetails` against a nonexistent barcode against the running container confirmed the exact placeholder shape (`{"barcode":"","description":"",...}`), and the rebuilt frontend image was checked to contain the fix.
- **`get_dashboard`'s always-true `||` (§0.5) / stray `""`-keyed entry (§7.3#8).** On inspection this was already fixed in the working tree — not by this session — as an undocumented side effect of the 7.1#13 dashboard-chart rebuild (which needed accurate real per-day data from `get_dashboard` and evidently got a pass over this function along the way). The condition now correctly reads `daily != "" && daily != "null"`. No code change made here; this entry exists to close out §0.5's and §7.3#8's stale "not fixed" callouts, which were never updated when the fix actually landed.
- **`predict_daily_sales`/`predict_weekly_sales` invalid SQL (§0.5's "newly-found" list).** Also already fixed, and in this case already correctly documented — see §7.4's 7.1#11 entry above. §0.5's own "newly found, not fixed" bullet was simply never updated to point there. No code change; cross-reference only.
- **`stockanalysis`/`salesanalysis` claiming embedded JSON is "incomplete" (§0.4's closing note) — actually fixed, not just mitigated.** Root-caused by direct experimentation against the live Boudica SLM deployment, bypassing boudica_pos entirely (`curl` straight to `/api/boudica/chat` and `/generate` with the exact same payload shapes `call_boudica()` sends):
  - `/chat`'s default `use_rag: true` was the first-order problem: `applied_rag_enabled: true` in the response confirmed it was silently substituting unrelated documents from the user's own RAG corpus (Nextcloud/Collabora integration docs, GitHub auth docs — real prior content, nothing to do with this request) instead of reading the embedded JSON at all.
  - Setting `"use_rag": false` in the request body fixed RAG substitution but exposed a second, independent problem: `/chat` is also session-aware by default and matched the request against a similar-looking prior call, replying "We had a related conversation from the past 10 days..." (`session_history_used: 1`) instead of the actual current data — and switching to the documented "session-less" `/generate` endpoint didn't avoid this either, so it isn't purely a `/chat`-specific quirk.
  - The user identified the actual fix: Boudica's inference engine hardcodes recognition of the literal directive phrases **"No Rag. No Memory."** in the message text itself (not just advisory prompt wording an LLM might ignore) — this is a rule enforced by the engine layer, confirmed by testing it eliminated the RAG/session-history substitution in isolation.
  - Even with RAG and session history correctly suppressed, a large JSON blob pasted inline into the `message` string was still being paraphrased/hallucinated rather than actually read (confirmed with byte-verified, definitely-not-truncated payloads that the model nonetheless claimed were "cut off"). The user suggested testing Boudica's file-upload path instead of inline text — per `BOUDICA_API_DEVELOPER_GUIDE.md`, `/chat` accepts `multipart/form-data` with a `file` part that gets text-extracted and injected as grounded document context, a fundamentally different code path from paraphrasing a long string. Live-tested: this was the one approach that produced a response correctly and specifically grounded in the actual attached data (correctly identified the single real stock item by name/quantity, or correctly reported zero sales for an empty period, instead of inventing plausible-sounding content).
  - **Implemented**: added `Http::post_multipart_with_file()` (`http.cpp`/`.h`) — an in-memory (no temp file on disk) multipart POST via libcurl's mime API, modeled on the existing `Http::upload()`. Added `call_boudica_with_data()` (`main.cpp`, alongside `call_boudica()`) which sends `"No Rag. No Memory. " + message` as the `message` field, `use_rag=false`, and the report JSON as a `file` part — `call_boudica()` itself is untouched, still used by `getadvice`/the four `predict_*` forecasts, none of which embed large structured data this way. `get_stock_analysis()`/`get_sales_analysis()` now call this instead, with the report JSON as the attached file instead of inline prompt text.
  - **Live-verified end-to-end through boudica_pos's own `stockanalysis`/`salesanalysis` commands** (not just direct Boudica calls): correctly summarized the one real seeded stock item by name and quantity with no fabricated thresholds or unrelated content; correctly reported an empty sales period as zero sales rather than hallucinating trends. `getadvice` (unmodified `call_boudica()` path) reconfirmed still working, unaffected by this change.

### 7.8 First real browser click-through of the till (2026-09-10) — two new bugs found and fixed

Every prior session's frontend verification (§7.6 included) was `curl`-only against the raw CGI contracts — no browser was available. This session finally had one (Chrome via X11 forwarding over SSH). Logged in as `admin` and worked through Till (barcode scan, cash sale, card-payment button), Dashboard, Product Lookup, and Stock Take by actually clicking, not just inspecting responses. Confirmed live in-browser, matching what curl-testing had already shown: barcode scanning adds the correct priced item (§7.1#3), a full cash sale completes end-to-end with stock update + receipt + till clear (§7.1#4/#6), the dashboard's Sales Comparison chart shows real data and the correct "no prior cashup" message (§7.1#13), and Product Lookup now shows "No product found" instead of a blank card (§7.7). Two genuine new bugs surfaced that no amount of curl testing could have caught, since both are about browser-side state/timing, not request/response shape:

- **Every till session's first barcode scan is silently corrupted.** `till.js` declared `let barcode;` (module-level, uninitialized — JS default is `undefined`, not `''`). The scanner's keyup handler does `barcode += ev.key` on every keystroke including the very first one after a page load; `undefined + '5'` string-coerces to the literal string `"undefined5"`, so the very first scan of every fresh till session looked up a barcode like `"undefined5000000000001"` instead of `"5000000000001"` — always a miss, with no indication why. Every subsequent scan in the same session was fine, since the variable gets reset to `''` after the first Tab/Enter. Live-reproduced, then fixed (`let barcode = '';`), then live-reconfirmed correct on a genuinely fresh session (`localStorage.clear()` + hard reload). **`stock_take.js` had the exact same bug** (`let stock_take_barcode;`, identical accumulator pattern) — found by inspection once the till.js pattern was recognized, fixed the same way, and live-reconfirmed on Stock Take's own scanner input. `add_stock_item.js`/`product_lookup.js`/`stock_in.js` already initialized their equivalent variables correctly (`= ''`) and don't have this bug.
- **The Workshops & Services quick-add panel never loads on a genuinely fresh login.** `till.js`'s `loadWorkshops()` runs unconditionally on `DOMContentLoaded`, before login — reproduced live: `console.error('Error fetching workshops:', ...)` → `"User is not active on the system"` at page load, before any credentials exist in `localStorage`. Nothing ever retried it after a successful login, so an operator's very first session showed no real workshop buttons (whatever static/stale markup was already in the DOM) until an unrelated page reload happened to occur after login. Fixed by following the exact pattern `login.js` already uses for the analogous `load_supplier_list()` call: `loadWorkshops()` (guarded with `typeof loadWorkshops === 'function'`, since `login.js` is shared across pages that don't define it) is now also called from both of `login.js`'s existing success paths — the top-of-file auto-login-from-stored-credentials check, and the login button's click handler — not just from `till.js`'s own `DOMContentLoaded`. Live-reconfirmed on a genuinely fresh session: `getworkshops` now succeeds right after login, and the quick-add grid populates with real DB-backed buttons (verified via their `dataset.description`/`dataset.price`, not just visually) instead of showing nothing/stale content.

Not exhaustively click-tested this pass: card payment beyond opening the modal (no Stripe test key in this environment, consistent with every other Stripe finding since §7.4), Special Orders, Invoice upload, Suppliers, and Forecast tabs.

### 7.9 Till rebranded to real Boudica colours/logo (2026-09-10)

Not a bug fix — a visual rebrand, at the user's request, of the till (`code/web/`) only (`code/web_store/`, the customer storefront, was explicitly deferred — see conversation). The till previously used OmniIndex branding (`assets/omniindex.png` header logo, a generic blue `#0066cc`/orange `#ff6b35` palette in `styles/modern.css`) left over from its origin as a template.

Sourced the real brand identity from `~/code/Boudica/boudica/boudica_slm/content/interfaces/html/chat_interface/assets/` rather than that repo's `main.css` (which turned out to still carry an old placeholder blue/green Material palette, `#4285f4`/`#34a853`, matching a simple circle-and-"B" `logo.svg`/`favicon.svg` — inconsistent with the actual deployed favicon). The real mark is `boudica.ico`/`logo.png`/`Original Logo.svg` — a gold hexagonal cube mark, colour-sampled directly from the pixels: navy `#25214e`, gold `#b8860b`. Notably, `#25214e` already existed hardcoded in several of this till's own **dead, unlinked** CSS files (`styles/login.css`, `styles/pages.css`, `styles/till.css` — none of `login.css`/`page.css`/`pages.css`/`reports.css`/`till.css`, nor the entire `styles_sib/` directory, are referenced from `index.html` at all; only `modern.css`+`toast.css` are live) — suggesting an earlier, abandoned pass at this same rebrand.

Changes (`code/web/index.html`, `code/web/styles/modern.css`):
- Copied `logo.png` → `assets/boudica-logo.png` (header logo + new login-screen logo) and `boudica.ico` → `assets/boudica-favicon.ico` (browser tab icon), replacing `omniindex.png`/`dropblock.ico`.
- `modern.css`'s `:root`: `--primary`/`--primary-dark`/`--primary-light` → the navy family (`#25214e`/`#17142e`/`#486586`); `--accent`/`--accent-light` → the gold family (`#b8860b`/`#d4a017`) — previously declared but **never actually used anywhere in the file**, so the redesign is the first time gold appears at all. Applied gold to `.tab-button.active` (was reusing `--primary`, which would have made active tabs invisible-ish navy-on-dark) and the login screen's primary "Login" button, plus a new 3px gold `border-bottom` on `.header-container`, for real accent contrast against the new navy rather than an all-navy reskin.
- Found and fixed one genuinely off-palette hardcoded color while at it: `index.html`'s "% Discount"/"Price Override" buttons had `style="background-color: #8abfeb"` (an unrelated sky-blue, not tied to any CSS variable) — switched to `var(--primary-light)`. Left the other inline per-button colors alone (`#3EDA95` mint-green on most "add/confirm" actions, `#f0ad4e` amber on Print Receipt/Accept-All, `#28a745` on Export CSV) since those are deliberate semantic status colors, not brand-primary leftovers, and don't clash with navy+gold.

**Live-verified in-browser** (login screen, header, Till, Dashboard, Reports, Stock — both logged-out and logged-in states, after a hard reload to bypass the same nginx/browser JS-caching issue hit earlier in this session): cohesive navy+gold look throughout, real gold hexagon logo rendering correctly against the navy header (confirmed transparent PNG background, no white-box artifact), login card's gold CTA button, gold active-tab underline. Not done: `code/web_store/` (deferred), and no attempt to touch the dead unlinked CSS files noted above (left as pre-existing, now even-more-clearly-obsolete cruft — worth a cleanup pass sometime, not attempted here).

### 7.10 Till interaction simplified for busy-period use (2026-09-10)

At the user's request — the till should stay fast and simple for a busy small shop rather than growing "all singing all dancing." Reviewed the numpad/payment interaction model in `till.js`/`keypad.js`/`index.html` end to end (not just a visual pass) and found one genuine safety bug plus several points of unnecessary ambiguity:

- **Real bug, not just UX: the numpad's "C" button silently wiped the entire in-progress sale, not just the number being typed.** `keypad.js`'s own click listener already correctly resets only its numeric display on `clear`; separately, `till.js`'s delegated listener on the same `#till` container *also* handled `action === 'clear'` by calling `clearSale()` (`currentSaleItems = []`) — both fired on the same click. A cashier correcting a mistyped manual price or discount percentage with the obvious "C" key was one tap away from silently losing every item already rung up, with zero confirmation and no undo. Removed the `clearSale()` call from that branch entirely — "C" now only ever clears the numpad's own display, exactly like a calculator's clear-entry key.
- **Voiding a whole sale is still possible, but now a deliberate, separate, confirmed action.** Replaced the numpad's "." key — dead code, `keypad.js`'s own comment already noted "this button is now redundant with automatic decimal placement. Do nothing." — with a new "Void Sale" button in that same grid slot, styled deliberately muted/neutral (not red like "C", so it doesn't visually compete for "act on me fast" attention) that requires a second tap within 3 seconds to actually confirm (`armOrConfirmVoidSale()`, `till.js`) — it flips to a red "Tap to confirm" state and silently reverts if the second tap doesn't come. Also removed the now-fully-unreachable `period`/`comma` dead-code branch from `keypad.js`.
- **The "x" (multiply) button — actually "set the quantity of whichever item was added last" — was genuinely ambiguous as a bare symbol.** Relabeled to "Qty" with a tooltip; no behavior change (still real-run findings for future work: it only ever targets the *last* non-refund, non-discount line, not an arbitrarily-earlier one, which could surprise an operator scanning A then B then trying to set A's quantity — not fixed this pass, flagged here for whoever wants to make it click-to-select-then-set instead).
- **Discount/Price Override visually competed with Cash/Card for attention** despite being rare, deliberate actions rather than the normal fast path. Restyled as a quieter outline/ghost `payment-btn-secondary` (transparent, brand-navy border/text, fills solid navy on hover) so Cash (solid green) and Card (solid navy) read as the obvious default at a glance, especially useful mid-rush.
- **Added a "gamified" tactile-feedback layer, cheap and reversible**: every payment/action button now has a consistent lift-on-hover/press-down-on-click animation (`.payment-btn:hover`/`:active`, previously only the numpad had this), and successfully adding/incrementing an item now flashes that receipt line gold for 600ms (`flashReceiptItem()`, keyed to the actual affected item's index, not just "the last row" — important since scanning an already-in-the-cart barcode increments its existing line wherever it sits, not necessarily the bottom one) — an instant, glanceable "yes, that worked" that doesn't require reading a toast mid-rush.

**Live-verified in-browser, not just read**: scanned a real item, confirmed the gold flash fires on the correct line; tapped "C" repeatedly and confirmed the sale survives; tapped "Void Sale" once and confirmed it arms (red, "Tap to confirm") without clearing anything, waited out the 3-second window and confirmed it silently reverts with the sale still intact, then tapped it twice in quick succession on a fresh sale and confirmed that genuinely clears the cart. Not attempted this pass (flagged, not fixed): a general "favourites/quick-add" tile grid for ordinary top-selling products — currently only Workshops & Services gets that fast-tap treatment; every other product needs a barcode scan or full numpad manual entry. Would need a new "favourite product" concept (schema + a dispatch command) — a bigger, separate task, not attempted here to keep this pass scoped to fixing/decluttering what already exists.

## 8. CORS lockdown + real session-token auth (2026-09-11)

At the user's explicit request, prompted by the production deployment shape: this backend runs on a PC physically in the shop (kept local deliberately, for cost and easy on-site support), but the public web store is tunneled back to it the same way this box already tunnels to the live My Boudica server for container registry pulls — meaning §3.8's `Access-Control-Allow-Origin: *` and the resend-credentials-on-every-call model (§3.6/§5/§6.2) are a real, currently-exploitable exposure once that tunnel is live, not a theoretical one for an internal LAN till.

**CORS (`main.cpp`, `int main()`):**
- The unconditional wildcard is gone. `main()` now builds an `Environment` object *before* writing any headers, reads the request's `Origin` (`HTTP_ORIGIN`), and reflects it back in `Access-Control-Allow-Origin` (with `Vary: Origin` and `Access-Control-Allow-Credentials: true`) only if it exact-matches an entry in a new `allowed_origins` config value (comma-separated). No match (including no `allowed_origins` configured at all, the default) means no `Access-Control-Allow-Origin` header is sent — same-origin callers are unaffected either way, since browsers don't consult CORS headers for same-origin requests.
- `Access-Control-Allow-Headers`/`-Methods` are now the specific values actually needed (`Content-Type, Authorization` / `GET, POST, OPTIONS`) rather than `*` (which isn't even spec-legal alongside `Access-Control-Allow-Credentials: true`).
- A genuine `OPTIONS` preflight is now answered directly (headers + 200, no command dispatch) instead of falling through to the GET-query-string path and erroring.
- Plumbed through `docker/backend/boudica_pos.conf.template`/`entrypoint.sh`/`docker-compose.yml` as `ALLOWED_ORIGINS` (default empty — this compose stack's till and web store both reach the backend same-origin through the frontend's own nginx proxy, so no CORS header is ever consulted locally; a production deployment tunneling the public web store back to this backend sets its real origin here).
- **Live-verified**: no `Origin` header → no ACAO header (unaffected). `Origin: https://evil.example.com` with `ALLOWED_ORIGINS` unset → no ACAO header. `ALLOWED_ORIGINS=https://shop.example.com` set, request with that exact `Origin` → reflected with `Vary: Origin` + credentials header; a different `Origin` in the same run → not reflected. A bare `OPTIONS` request → `200` with the CORS headers, no body needed.

**Session tokens, replacing resend-username-and-password-on-every-call (`main.cpp`, `authentication.cpp` untouched, `code/sql/setup.sql`, till frontend):**
- New `store.sessions` table (`user_id`, `token`, `created_at`, `expires_at`, `last_seen_at`, `revoked_at`) — added to the canonical `code/sql/setup.sql` and applied live to the running container DB. (In resyncing this, also found and fixed unrelated drift: `docker/sql/00-setup.sql.template` — the schema actually used to init a fresh container from a wiped volume — was missing the `attachment_filename` column and the entire `store.special_orders` table from §7.5, despite both being "applied live" and documented as shipped; a fresh install would silently have lacked both. Regenerated the template from the canonical file plus its one intentional local-only addition (the `store` role's password) so a wiped-volume rebuild now actually matches what's documented as working.)
- `generate_session_token()` (`main.cpp`) makes a 32-byte token via OpenSSL's `RAND_bytes` (already linked, no new dependency), hex-encoded — deliberately not `Crypto::string_create_id()` (seeded from the current second, guessable, wrong tool for anything that must be unforgeable).
- `login` now inserts a session row (12-hour expiry — a full shop shift) and returns `token`/`expires_in_seconds` alongside the existing user fields.
- New `check_session_token()` (mirrors `check_user_credentials()`'s shape/call sites exactly) validates a token against `store.sessions` (not revoked, not expired, user still active) and slides `expires_at` forward another 12 hours on every use — an actively-used till doesn't get logged out mid-shift; an abandoned token still dies at its last-refreshed mark.
- The main dispatch gate now accepts *either* a `token` *or* a resent `username`+`password` (`login`/`logout` are exempt, handling their own field checks) — additive, not a breaking change: the web store and any other not-yet-migrated caller keep working exactly as before, unauthenticated by anything but a real credential either way.
- New `logout` command revokes a token (`revoked_at = now()`) so it dies immediately rather than lingering to its natural 12-hour expiry after a till operator logs out.
- The 5 `webstoreorder`/`getreceipt`/`orderhistory`-family `customer_email = email_address` assignments now fall back to the authenticated user's own email (`json_str(jUser, "email")`) when a caller authenticates by token rather than resent username — dormant today (these commands are web-store-only and the web store hasn't migrated to tokens), but correct for whenever it does.
- **Till frontend migrated to tokens, and stopped storing the password entirely**: `login.js` stores `token` (never `password`, not even transiently beyond the login call) and calls the new `logout` command before clearing `localStorage` on the exit button. `application.js`'s shared `apiCall()` (the single choke point every till script but one already went through, per §7.6) now sends `token` instead of `password`. The thirteen call sites across `eod.js`, `invoice_in.js`, `stock_in.js`, `add_stock_item.js`, `stock_take.js`, `special_orders.js`, `product_lookup.js`, `order-history.js`, `reports.js` that gated on `localStorage`'s `password` now gate on `token` instead. `forecasting.js` — the one till script `apiCall` migration missed in §7.6, because it never used `apiCall` at all — had its own separate raw-`fetch`-with-username-and-password-in-a-GET-query-string path (plus a hardcoded absolute `http://localhost/cgi-bin/boudica_pos`, §7.3's 4th nice-to-have) for all four forecast commands; migrated to `apiCall()`, which incidentally closes that nice-to-have too. `show_products.js`'s "Show Products" supplier filter is the one leftover reference to a password: it calls `/cgi-bin/pgbcadmin`, a *different*, unrelated legacy admin CGI from a prior deployment (`code/thecuriositycabins/`, not shipped in `docker/backend`, already a 404 in this stack regardless of credentials) — left alone rather than migrated, since that CGI doesn't know about `store.sessions`; the dangling variable reference was fixed so it fails the same way (a 404) instead of a `ReferenceError`.
- **Live-verified end-to-end** against the running container: `login` returns a 64-hex-char token; the token authenticates a real command (`getsupplierlist`) with no password sent; a missing token *and* missing username/password is rejected with a clear error; an invalid token is rejected distinctly from a missing one; the legacy username+password path still works unchanged (confirms the web store isn't broken by this); `logout` revokes the token, and the same token is rejected immediately afterward. The built frontend image was checked to contain every JS change (not just the source tree). No browser was available this session to click through the till login UI itself (extension not connected) — flagged the same way every prior browser-dependent finding in this document has been when a browser wasn't available.

**Not attempted this pass**: migrating the web store (`code/web_store/`) to tokens, or removing its still-hardcoded `web_store_user`/`web_store_pass` service-account credentials (§3.3/§3.13) — those are separate, already-flagged findings, deliberately left as-is so this pass stayed additive and didn't risk breaking the storefront's working checkout flow.

## 9. Four remaining §7 findings closed out (2026-09-11, same session as §8)

At the user's request: stored-XSS (§3.7/§7.3), the geo-IP TLS/API-key issue (§3.15), and RBAC graduation (§7.1#8's binary tier) — plus confirming §3.6/§7.6's password-in-`localStorage` item was already fully closed by §8 earlier the same session (verified live: `grep` across every `code/web/scripts/*.js` for any remaining `localStorage`/password reference outside `login.js`'s one-time login exchange found none).

### 9.1 Stored-XSS — till-side, `escapeHtml()` added and applied at every real injection point

Scoped to `code/web/` (the till) per the user's original ask; `code/web_store/`'s `app.js`/`cart.js` (mentioned in §3.7's original citation) is a separate, already-flagged, not-yet-touched surface. Added a shared `escapeHtml()` (`application.js`, loaded before every other till script) and swept every `.innerHTML =` assignment across all 19 till scripts for unescaped DB-sourced or user-typed content — most (`dashboard.js`, `eod.js`, `stock_in.js`, `add_stock_item.js`, `invoice_in.js`, `special_orders.js`, `login.js`, `keypad.js`, `tabs.js`, `printer.js`) turned out to already be safe (either no `innerHTML` use at all, or only numbers/dates/static strings interpolated) — confirmed by reading each, not assumed. Real findings, fixed:

- **`product_lookup.js`** (the file named in the original finding) — `resultsHtml`'s product card interpolated `description`/`color`/`type`/`supplier`/`barcode`/`stock` raw; only `description` had a half-measure (`.replace(/"/g, '&quot;')`, attribute-only, and not applied to the visible text version of the same field). All six now go through `escapeHtml()`; the two "No product found" messages also escape the user-typed search barcode.
- **`till.js`** — the Workshops & Services quick-add button (§7.8) interpolated `workshop.title` (DB-sourced) raw into `innerHTML`.
- **`stock_take.js`** — the completion screen's per-item card, plus a real second bug found while fixing it: the "Update Stock" button used `onclick="updateStock('${iter.barcode}', '${safeSupplier}')"` (`safeSupplier` only backslash-escaped single quotes, `barcode` wasn't escaped at all) — **HTML-entity-escaping alone would not have made this safe**, since the browser decodes entities in an attribute back to the original text before an inline handler runs, so a stray quote in either field could still break out of the embedded JS string literal regardless. Replaced with a `data-barcode` attribute read via `.dataset` in a delegated click listener, which sidesteps the double-escaping problem entirely rather than trying to get it exactly right inline. Also dropped `supplier` from `updateStock()`'s signature — it was threaded all the way through the vulnerable onclick-parsing dance in the "Accept All" handler and never actually used in the API call.
- **`order-history.js`** — the same inline-onclick-with-embedded-DB-data pattern, four separate times (`viewOrderDetails(...)`/`viewOrderReceipt(...)` in the orders table, `viewOrderReceipt(...)` in the order-details modal, `printOrderReceipt(...)`/`downloadReceiptPDF(...)` in the receipt modal), plus plain unescaped text interpolation of `order_id`/`order_status`/`payment_method`/item descriptions in both modals. All four onclick sites converted to `data-*` attributes plus a delegated listener on the relevant container/modal; all text interpolation escaped. Also escaped `orderId` in the two dynamically-`document.write()`'d print-window `<title>` tags — a `document.write()`'d string is parsed as full markup, so an unescaped value there could break out of `<title>` into the surrounding print-window document, not just render literally.
- **`show_products.js`** — the (already separately-flagged-as-orphaned, §3.13-adjacent) "Show Products" supplier filter's product table interpolated `description`/`barcode` raw into text, a `value="..."` attribute, and a `data-barcode` attribute. Fixed for defense-in-depth even though this specific call already 404s in this stack (calls a different legacy CGI, see §8's closing note).
- **`reports.js`** — sales/inventory report tables interpolated `item.description`/`item.supplier`/`item.quantity` raw.
- **`forecasting.js`** — every AI-generated field (`forecast.explanation`/`confidence_level`/`trend`/`seasonal_trend`, `prediction.explanation`/`priority`/`recommended_quantity`, `itemData.product_description`) was inserted unescaped — the closest thing in this codebase to the original finding's "prompt-injected AI response" scenario, since these strings come back from the Boudica SLM. Also escaped the user-typed barcode in the reorder-prediction label.

**Not found**: the original finding's specific citation of `app.js:118-119` (`messageElement.innerHTML = htmlContent` for a raw AI advice response) — no file named `app.js` exists in `code/web/`, and no current till page has a `getadvice`/`stockanalysis`/`salesanalysis` UI at all (confirmed via `grep` across every till script and `index.html`). That citation was either always about the web store's `code/web_store/js/app.js` (a separate, out-of-scope file) or describes UI that existed in an earlier version of this codebase and isn't present now.

Rebuilt and reconfirmed the built frontend image contains every fix (not just the source tree, `grep -c escapeHtml` per file against the running container). Not click-tested in a browser this session (extension not connected) — the actual escaping logic (`&`, `<`, `>`, `"`, `'` → entities) was exercised directly, not just read.

### 9.2 Outbound TLS verification + hardcoded RapidAPI key (`http.cpp`)

`Http::get_geo()` (the geo-IP lookup) and the separate, equally-unused `Http::request()` both explicitly disabled certificate/hostname verification (`CURLOPT_SSL_VERIFYPEER`/`CURLOPT_SSL_VERIFYHOST` set to `0L`) and `get_geo()` hardcoded a live RapidAPI key in source. Confirmed via `grep` that neither function has any caller anywhere in `main.cpp` — this is unused, unwired code (matches the general pattern of several other unwired-but-present features in this codebase, e.g. §3.14's orphaned search functions), so nothing currently exercises this path in production. Fixed rather than deleted, since it's plausible future functionality and the fix is small: both functions now set `CURLOPT_SSL_VERIFYPEER, 1L` / `CURLOPT_SSL_VERIFYHOST, 2L` explicitly (matching libcurl's own secure defaults, which `post_json()`/`post_multipart_with_file()` — the methods actually used for real outbound calls to Boudica — already relied on implicitly without ever having been made to disable them). `get_geo()`'s signature now takes the RapidAPI key as a parameter instead of a literal, following the same config-injection pattern as `StripePayment`'s constructor; no caller exists yet to wire it to `get_configuration()`, so no new config key was added speculatively — whoever eventually calls this should source the key from config, not `getenv()` or a literal, consistent with every other secret in this codebase. Rebuilt cleanly; not runtime-exercised beyond compilation, since there's no live caller to exercise.

### 9.3 RBAC split into three real tiers (was: one binary gate)

`user_is_privileged()` (added in §7.4) treated `admin` and `manager` identically across all 9 gated commands — role was tracked and checked correctly, but nothing actually differentiated the two roles from each other, which is what the audit's "not role-graduated" finding was pointing at. Investigating `adduser` specifically (as a candidate for "should require admin, since account creation is a privilege-escalation risk") turned up that it's a red herring: `add_user()` inserts into `store.customers`, not `store.users` — it's the same customer-record/no-real-auth-path gap already documented in §0.3, not a staff-account-creation command at all, and nothing in either frontend calls it. So there is no actual "create a privileged account" capability in this backend today, gated or not.

Asked the user how the three roles should actually differ (a business-policy call, not a technical one) and implemented their choice:

- Added `user_is_admin()` (role `== "admin"` only) alongside the existing `user_is_privileged()` (`admin`/`manager`).
- `salesreport`, `revenuereport`, `taxsummary`, `adduser` now require `user_is_admin()` — tightened from manager-or-above to admin-only.
- `inventoryreport`'s gate was removed entirely — stock levels aren't the confidential figure the other three reports are, and any active till user benefits from seeing them.
- `addproduct`, `addsupplier`, `addinvoice`, `process_refund` stay at `user_is_privileged()` (manager or admin) — unchanged.

**Live-verified** against the running container with three real accounts (`admin`/`admin`, a throwaway `manager`-role account, and the existing seeded `testuser`/`operator`): `inventoryreport` succeeds for all three; `salesreport`/`revenuereport`/`taxsummary` succeed for admin and are cleanly rejected for both manager and operator; `addsupplier`/`process_refund` succeed (past the permission check, onto their own field-validation errors) for admin and manager and are rejected for operator. The throwaway manager account was deleted after testing; the seeded `testuser` account's password was reset to a known value to test with (pre-existing test-only account, not used by any real workflow).

## 10. §7.3 nice-to-haves closed out, plus one new finding fixed in the same pass (2026-09-11, same day as §8/§9)

### 10.1 Printer/store branding and the Star WebPRNT URL made configurable (§7.3#2/#3)

`printer.js` hardcoded "Curiosity Cabin" / "www.thecuriositycabins.com" on every receipt (both the cash-sale receipt and the special-order receipt) and `http://localhost:8001/StarWebPRNT/SendMessage` as the printer endpoint — all three leftover from a specific prior deployment, none configurable for a fresh install of this template. Extended the existing `getpublicconfig` command (added in §7.6 for the Stripe publishable key — same "non-secret, client-safe config" reasoning applies) with `star_printer_url`/`store_name`/`store_website`, each backed by a new `get_configuration()` key and, for `star_printer_url`/`store_name`, a same-as-before default (`http://localhost:8001/StarWebPRNT/SendMessage` / `"Boudica POS"`) if left unconfigured — so an un-set-up fresh install behaves exactly as before, just no longer from a literal in source. Plumbed through `docker/backend/boudica_pos.conf.template`/`entrypoint.sh`/`docker-compose.yml` as `STAR_PRINTER_URL`/`STORE_NAME`/`STORE_WEBSITE`.

`printer.js` gained `loadPrinterConfig()` (same lazy-fetch-and-cache pattern as `till-card-payment.js`'s `loadStripePublishableKey()`), and both `drawReceiptContent()`/`drawSpecialOrderReceiptContent()` (and `sendCanvasToPrinter()`, for the URL) now `await` it instead of using literals; the "Visit our Website" line is only printed at all when `store_website` is actually configured, rather than always showing a specific business's domain. `order-history.js`'s receipt-modal header (`<h3>The Curiosity Cabins</h3>`) reuses the same cached config rather than duplicating the fetch. The till's page `<title>` tag (`Boudica POS - The Curiosity Cabins`, not caught by §7.9's rebrand pass since that was a colour/logo pass, not a text sweep) is now just `Boudica POS`.

**Live-verified** against the running container: `getpublicconfig` returns the same-as-before defaults when unconfigured (`star_printer_url: "http://localhost:8001/StarWebPRNT/SendMessage"`, `store_name: "Boudica POS"`, `store_website: ""`); setting `STORE_NAME`/`STORE_WEBSITE`/`STAR_PRINTER_URL` env vars and restarting the backend container immediately changes what `getpublicconfig` returns, confirmed via a real request with each override in place. Not click-tested in a browser this session (extension not connected) — the actual receipt-drawing code path wasn't visually confirmed, only the config plumbing it depends on.

### 10.2 Printer/cash-drawer failures no longer silent (§7.3#1)

Three separate places in `sendCanvasToPrinter()` swallowed failures with no operator-visible feedback at all: the "Printing Failed" case used a blocking `alert()` (bad UX on a busy till, and the kind of dialog that freezes the page until dismissed); the printer-communication-error and outer-exception paths had their `alert()` calls commented out entirely, so those two failed completely silently; and `drawReceiptContent()`'s cash-drawer-open command had an empty `catch {}`. All four now surface a concise `showToast(..., 'error')` (non-blocking, consistent with every other error in this codebase) with the full technical detail (`response`, `error`, `url`) logged to the console for whoever's actually troubleshooting the printer, rather than a multi-line dump in a blocking dialog or nothing at all.

### 10.3 Dead code removed (§7.3#7)

`till-card-payment.js`'s `addCardPaymentOption()`/`handleCardPaymentClick()` — confirmed via `grep` across every till script and `index.html` to have zero callers (the audit's own suspicion that `handleCardPaymentClick`'s `localStorage.getItem('currentOperator')` read a key nothing ever sets was correct — it's simply never reached) — deleted, along with their entry in the `window.tillCardPayment` export object. `scripts/star/jquery-1.6.4.min.js`, `scripts/star/jquery.mobile-1.0rc2.min.js`, `StarBarcodeEncoder.js`, `StarWebPrintDisplayBuilder.js`, `StarWebPrintExtManager.js` — confirmed via `grep` to have no `<script src>` in `index.html` and no dynamic reference anywhere — deleted; `StarWebPrintBuilder.js`/`StarWebPrintTrader.js` (the two actually loaded and used) are untouched.

### 10.4 New finding, same pass: Void Sale's arm window didn't account for the sale changing mid-arm

Spotted while reviewing this list (not previously logged): `armOrConfirmVoidSale()`'s (§7.10) 3-second confirm window compares nothing about the sale's actual contents between the first and second tap — a cashier tapping Void once, then scanning another item during the window, then tapping again would silently void the sale *including* the just-scanned item, with no extra warning that anything had changed since arming. Fixed by snapshotting `currentSaleItems.length` when the button arms; if the count differs on the second tap, it re-arms (with a "Sale changed — tap again to confirm void." toast) instead of voiding, so a stale confirm tap can't wipe out an item the cashier hasn't actually seen and confirmed. This is a count-based check, not a deep-equality one — swapping the refund flag on an existing line, or scanning an already-in-cart barcode (which increments an existing line's quantity rather than adding a new one), wouldn't change the count and so wouldn't retrigger the guard; flagged here as the known limit of this fix rather than chased further, since the original finding was specifically about *added* items. Verified in isolation (the arm/re-arm/confirm state machine extracted and run under Node, not just read) against the exact scenario described: arm → item added mid-window → confirm-tap re-arms instead of voiding → next confirm-tap genuinely voids the then-current (2-item) sale.

**Live-verified for 10.1–10.3**: backend rebuilt and confirmed via the running container (config defaults and overrides both correct); frontend rebuilt and confirmed via the built image (dead files absent, no remaining hardcoded branding strings outside explanatory comments, `till-card-payment.js`'s dead functions absent). Every touched till script (`printer.js`, `order-history.js`, `till.js`, `till-card-payment.js`, and the eight others touched in §9) passed `node --check` for syntax validity — no browser was available this session to click through the till UI itself.

## 11. Customer kiosk rebuilt around real stock data + a no-code branding form (2026-09-12)

`code/kiosk/`'s original GrapesJS drag-and-drop layout builder (reviewed §-less, see `project_boudica_kiosk_review` memory) was the wrong tool for the actual goal: a shop owner with no web-design skill needs to brand their own customer-facing kiosk, not lay out page elements by hand. It also had no real connection to stock data at all — the "layout" was just arbitrary HTML a staff member typed into the editor.

Rebuilt around three pieces:

**1. A new backend command, `getcatalog` (`main.cpp`, `get_catalog()`)** — a customer-safe, paginated product browse/search. Deliberately returns only fields safe to show a customer (`barcode`, `description`, `color`, `type`, `price`, `image_url`) plus a coarse `availability` tier (`in_stock`/`low_stock`/`out_of_stock` off `store.stock.available`, threshold 5) rather than `purchase_price`, `supplier`, or the exact stock count. Ungated (any active session, like `inventoryreport` per §9.3's reasoning — stock levels aren't the confidential figure). A new `kiosk_user` (`operator` role) service account is seeded (`docker/sql/02-seed-users.sql.template`, `KIOSK_PASSWORD` env var) for the kiosk to log into via the normal §8 session-token flow — it never touches the DB directly or holds a native Postgres credential. Found and fixed the same "empty result still returns one placeholder row" quirk as `get_dashboard` (§7.3#8)/`getdetails` (§7.7) along the way — an empty `getcatalog` search returned one fake blank-fields product until filtered out.

**2. `code/kiosk/kiosk-system` rewritten**: `admin.html`'s GrapesJS canvas replaced with a short guided form (store name, tagline, primary/accent colour pickers, a logo upload, and the AI chat's welcome message) — POSTed to new `/api/branding` and `/api/branding/logo` endpoints (both behind the existing Basic Auth gate; `multer`, memory storage, 2MB limit, image-mimetype allowlist). `index.html` replaced with a fixed two-tab template (Browse Stock / Ask Boudica AI) driven entirely by that branding data (CSS custom properties for colour, logo/name/tagline in the header) — no arbitrary HTML is ever stored or rendered, closing off the stored-content-injection angle the old `/api/save-layout` had by construction, not by sanitizing it. "Browse Stock" calls a new `/api/catalog` endpoint (search box, debounced, paginated "Show more") that server-side logs into boudica_pos as `kiosk_user` and calls `getcatalog` — real live price/stock/availability, not fabricated data.

**3. AI chat grounded in live stock, not just RAG.** `getcatalog` only does a single substring `ILIKE`, so passing a customer's whole sentence ("do you have any wool in stock?") as the search term never matches a product description — fixed by trying the full message first, then falling back to significant individual words (a small stopword-filtered keyword extractor, `extractKeywords()`/`findCatalogMatches()`), merging/deduping matches by barcode. Up to 5 real matches (with actual price/availability) get embedded into the message sent to Boudica alongside the customer's own question; `use_rag` stays `true` on top of that, deliberately — unlike the reporting endpoints' §7.7 grounding-suppression fix, this is genuine customer Q&A meant to draw on both live stock data and the shop's own RAG corpus (workshops, project guides) — see `project-boudica-pos-opensource-plan` memory, this is an intentional product feature, not a bug to suppress.

**Live-verified end-to-end**, not just read: `getcatalog` tested directly against the running backend (search, pagination, empty-result, availability tiers all correct) and through the kiosk's own proxy; branding save/logo-upload tested with/without auth (401 unauthenticated, 200 with correct credentials, 503 with `KIOSK_ADMIN_PASSWORD` unset entirely); a real chat call against the live Boudica deployment for "Do you have any wool in stock and how much is it?" correctly cited the actual in-stock item's real price (£8.99) and distinguished it from a same-description out-of-stock row, confirming the keyword-fallback grounding actually works, not just that the code compiles. No browser was available this session (extension not connected) — the visual result (card grid layout, tab switching, colour theming) was not click-tested, only every HTTP contract it depends on.

**Noted, not chased**: one grounded chat response's reasoning trace referenced "a related conversation from the past 10 days" — traced to RAG pulling in unrelated documents from this same shared dev/test Boudica account's prior, unrelated testing sessions (GitHub auth, Nextcloud, database schema topics), not a bug in the kiosk's per-request `session_id` handling. A real deployment's dedicated single-shop Boudica account (per the open-source bundling plan) wouldn't have this cross-contamination — not fixed or investigated further this session since it's a property of the shared dev test account, not shippable kiosk code.

### 11.1 "Browse Stock" made semantic: multi-word matching + an AI fallback for conceptual questions

Same session, immediately after §11: the user asked for Browse Stock to handle queries like "do you have any blue aran 4 ply?" (a structured multi-attribute query) and "do you have any items suitable to create a craft bear?" (a genuinely conceptual one no keyword search could ever answer).

**Backend: `getcatalog` now does AND-across-words, OR-across-fields matching** (`get_catalog()`, `main.cpp`). The original implementation ran the *entire* search string as one substring `ILIKE` across `description`/`color`/`type`/`barcode` — "blue aran" could never match, since no single column contains both words (color="Blue", description="...Aran..." are separate columns). Now each word is required to match *some* field independently, all words ANDed together. Live-verified: "blue aran" now correctly finds a blue Aran product; "aran 4 ply" finds both colourways of a 4-ply Aran yarn.

**Kiosk: a new `/api/smart-search` endpoint, layered on top of the fast search** (`server.js`). Tries the real `getcatalog` search first (free, instant — handles the "blue aran 4 ply" case with the fix above). Only when that returns zero results does it fall back to AI: fetches a candidate pool (up to 150 products, paginated `getcatalog` calls) and asks Boudica to pick relevant barcodes given the customer's own words.

Two real bugs found and fixed while building this, both only visible by actually calling it, not by reading the code:
- **Inline embedded catalog data got ignored.** Sending the candidate list as JSON inside the `message` field produced "the customer's question is missing" even though both the question and the data were plainly present — the same "doesn't trust/use inline embedded text" behavior `CODE_VERIFIED_AUDIT.md` §7.7 already hit and fixed for `stockanalysis`/`salesanalysis`. Fixed the same way: the candidate list is sent as a multipart file attachment (`post_multipart_with_file`'s pattern from `call_boudica_with_data()`, replicated in Node via `FormData`/`Blob`), with `use_rag: false` and a "No Rag. No Memory." prefix so the structured pick-from-this-list task isn't distracted by RAG documents (deliberately different from §11's main customer-facing chat, which keeps RAG on).
- **`max_tokens` was too low once there was real reasoning to do.** With a 2-item candidate pool the model's reply came back complete; with 7+ candidates the same `max_tokens: 400` cut the response off mid-JSON (`{"items": ["`) — the model's hidden chain-of-thought (still counted against the token budget) ran longer with more to reason about, leaving no budget for the actual answer. Raised to 1500, confirmed complete, valid JSON with up to 13 real candidates.

The UI (`index.html`) keeps instant debounced typing against the plain `/api/catalog` filter (unchanged, free) for fast browsing, and adds an explicit Search button/Enter-key action that calls `/api/smart-search` instead — deliberately not on every keystroke, since the AI fallback takes several seconds. A short "Boudica AI: ..." note banner shows the model's one-line rationale when it picked results (`mode: "ai"` in the response); plain keyword hits (`mode: "direct"`) show with no banner.

**Live-verified end-to-end** against the real running stack: "blue aran 4 ply" resolves via the fast path (`mode: "direct"`, no AI call, no note); "do you have any items suitable to create a craft bear?" correctly falls through to AI and returns real stock (polyester stuffing, safety eyes, yarn) with real prices/availability and a sensible one-line explanation, `mode: "ai"`, ~10 seconds end to end. No browser was available this session — the new search UI (button, note banner, loading state) was verified by API contract only, not click-tested.

**Known limitation, not chased**: the AI fallback's candidate pool caps at 150 products (a few `getcatalog` pages) — a shop with a much larger catalog than that would only have part of it visible to the AI-assisted search. Fine for a small single-till shop (this project's stated scope); would need revisiting for a bigger catalog.

## 12. `code/web_store` security pass — never audited before (2026-09-12)

The user's actual goal for `code/web_store` is to grow it into (1) a general marketing site, (2) a real online-order storefront, (3) a kiosk-style mode like `code/kiosk`, and (4) a Boudica-moderated customer community. Before any of that, the user asked for a security-first pass on what's already there — this directory had never had a dedicated audit (only the till, `code/web/`, had). It turned out worse than the till: three different fictional brand names in one page, a client-side raw-SQL layer, real stored-XSS, and — found only by actually trying to fix it — three pages whose entire JavaScript had never executed at all.

### 12.1 Deleted: a client-side raw-SQL layer (`js/driver/pgbc.js` + friends)

`js/data.js` built SQL strings **in the browser** (`SELECT ... WHERE ${category.whereClause}`) and shipped them to a `pgbcadmin` CGI endpoint — a completely different, unrelated OmniIndex product not part of this repo — authenticated with a real-looking hardcoded personal credential (`sibain@omniindex.io` / a real-looking password) pointed at `https://demo.pgbc.ai:5435`. `pgbcadmin` isn't reachable from this stack, and `store.web_store_front` (the table the "featured categories" query needed) exists in the schema but was never seeded — this layer has never actually worked here, by design as much as by bug. Deleted entirely rather than fixed: `js/driver/pgbc.js`, `js/driver/api.js` (fake `createuser`/`registerinitialuser`/`updatepassword` — not real backend commands, confirmed by diffing against every `main.cpp` dispatch entry), `js/driver/results.js`, `js/driver/credentials.js` and `js/driver/storage.js` (both dead once nothing else imported them), `js/driver/` itself, `js/users.js` (dead code, imported nowhere), and top-level `js/storage.js` (same). `data.js`'s two real jobs — homepage "featured categories" and per-category browsing — are rebuilt on the kiosk's real `getcatalog` command instead (§11), grouping by product `type` client-side in place of the raw-SQL-clause table.

### 12.2 New shared module: `js/session.js` — one credential, one place, session tokens

Every page used to carry its own copy of `web_store_user`/`web_store_pass` and send it, in the clear, in a GET query string, to `https://demo.pgbc.ai` on every single request. `js/session.js` is now the one place that credential lives: it logs in once per browser session via the real `login` command (§8's session-token flow), caches the token in `sessionStorage`, and exposes `apiCall(command, params)` (POST, same-origin `/cgi-bin/boudica_pos`, auto-retries once on token rejection) and `escapeHtml(value)` for every other file to use. **This is a declared, deliberate tradeoff, not an oversight**: the user was offered a kiosk-style Node backend to hide the credential server-side entirely (as `code/kiosk` does) and chose to keep the storefront static instead — an anonymous shopper still has to be able to browse before any real login exists, and a static site has no server of its own to hide a password behind. What changed: the password is sent once per session instead of per-request, POSTed instead of URL-embedded, and never leaves this origin; the *token*, not the password, is what every other call actually uses. `js/epos.js`, `js/data.js`, `js/cart.js`, and `js/stripe-checkout.js` were all migrated onto this module, deleting their own hardcoded copies of the credential and the external host.

### 12.3 Real, previously-undiscovered bug: every web-store order was attributed to the same fake customer

`webstoreorder`/`orderhistory` derived `customer_email` from the *caller's own auth identity* (`email_address`/`jUser`'s email), not a real customer-supplied address. Since the web store always authenticates as the shared `web_store_user` account, **every single order was recorded under the literal string `"web_store_user"`**, and `orderhistory` returned that same shared bucket to *every visitor* — any two customers using `order-history.html` saw each other's combined order history. Fixed (`main.cpp`) by adding a dedicated `email` parameter, distinct from the auth username, for both commands — live-verified with two real accounts (`alice@example.com`/`bob@example.com`) getting correctly isolated, empty-until-populated histories.

That fix then surfaced a second, previously-masked bug: `customer_orders.email` is a foreign key into `store.customers`, and nothing in the web store can create that row — `adduser` is admin-gated (this session's own §9.3 RBAC tightening) and the web store's service account is only `operator`. A real customer email now failed the order insert outright instead of silently succeeding with wrong data. Fixed by having `record_web_store_order()` upsert a minimal guest row (email only, no password — can never be used to log in) before inserting the order, at the user's explicit choice (offered leaving checkout blocked as the alternative). Also swapped an unguarded `std::stod` on the client-supplied order total for the existing `safe_stod` helper while in this function. Live-verified end-to-end: a real order for a brand-new email now succeeds, and shows up correctly (and *only*) in that email's own order history.

### 12.4 Real, previously-undiscovered bug: three pages' JavaScript had never executed

`order-history.html`, `receipt.html`, and `cart-management.html` each had a `<script>` tag — **missing `type="module"`** — containing a top-level `import Toast from './toast.js'` statement. A classic (non-module) script throws a `SyntaxError` on a top-level `import` and simply never runs; there's no visible page-breakage, just silent failure in the console. All three pages have been completely non-functional, in any environment, for as long as they've existed. (`receipt.html` even had `window.printReceipt = printReceipt;`-style manual global exports at the bottom — someone had already anticipated the module-scope problem for *those* two functions specifically, but the real blocker was the missing `type="module"` upstream of that.) Fixed by adding `type="module"` and correcting the now-wrong-in-context relative import paths (`./toast.js` → `./js/toast.js`, since these pages live at the store root while the JS lives under `js/`).

Fixing *that* was a precondition for fixing anything else in these files, since none of their code had ever actually run. Once live:

- **Same hardcoded-credential/external-host pattern** in all three, migrated to `js/session.js`'s `apiCall()`.
- **Same inline-`onclick`-with-embedded-order-data XSS pattern** the till already hit and fixed in §9.1 (`onclick="viewOrderDetails('${order.order_id}', '${order.items.replace(/'/g, "\\'")}', ...)"`) — HTML-entity-escaping alone doesn't make this safe, since the browser decodes entities back to the original text before an inline handler runs. This one is real and reachable: `items` is client-supplied cart JSON at `webstoreorder` time, stored verbatim, and rendered back here. Replaced with `data-*` attributes plus delegated click listeners (order-history.html's order-details/receipt-link buttons, cart-management.html's load/delete-cart and wishlist add/remove buttons).
- **Every other rendered field** (order ID, dates, statuses, item descriptions, receipt fields, saved-cart names, wishlist items) now goes through `escapeHtml()`.

### 12.5 Stored-XSS in the Boudica advice chat (`index.html`/`js/app.js`) — the citation §3.7/§9.1 could never confirm

`appendChatMessage()` rendered Boudica's response via raw `innerHTML` — this is the exact `app.js:118-119` citation the original audit (§3.7) and the till's XSS sweep (§9.1) both flagged but could never locate a matching file for ("no file named `app.js` exists in `code/web/`"). It was always this file, `code/web_store/js/app.js`, out of scope for those till-only passes. Unlike the kiosk/till's plain-text AI responses, `get_advice()` (`main.cpp`) deliberately asks Boudica to *"Format as HTML please"* for genuine formatting (headings, bold ingredient lists, etc.) — so escaping to plain text would break the feature, not just fix the bug. Fixed with DOMPurify (loaded via CDN, `index.html`) instead: strips dangerous constructs (script tags, event handlers, `javascript:` URLs) while preserving real formatting. The customer's *own* typed chat message (also previously raw-`innerHTML`'d into `<p>${prompt}</p>` — a self-XSS vector) now goes through `escapeHtml()` instead. Also fixed the same class of unescaped-`innerHTML` issue in `displayProducts()`/`renderProductListInModal()` (product name/image/searchTerm) and `cart.js`'s `displayCart()`/`updateSavedCartsList()` (item name/image, saved-cart names).

### 12.6 Other fixes in the same pass

- **`reports.html` deleted outright.** A fully public, unauthenticated page calling `salesreport`/`revenuereport`/`taxsummary`/`inventoryreport` as the shared `web_store_user` account. This session's earlier §9.3 RBAC tightening (admin-only for the first three) already closes most of the practical exposure, but a customer-facing "store admin dashboard" page has no legitimate reason to exist on the public storefront at all — the till already has real reporting behind real staff login. No functionality lost: nothing links to it from anywhere a customer would go.
- **Stripe placeholder key fixed.** `stripe-checkout.js` hardcoded `pk_test_51234567890`. Now fetches the real key from `getpublicconfig` at first use (same pattern the till uses, §7.6), via `js/session.js`.
- **Obsolete Docker-build sed patches removed** (`docker/frontend/Dockerfile`). A previous pass had patched `https://demo.pgbc.ai`/`http://localhost/cgi-bin` out of the built image only, without fixing the tracked source. Both are now fixed at the source (this section; §8 for the till's), so the patches had nothing left to match — removed.
- **`README.md` corrected** where it actively misled: referenced a nonexistent React (`REACT_APP_*`) setup for a vanilla-JS/no-build-step project, a GitHub clone URL for a different repository, deleted `js/driver/*` files, the deleted `reports.html`, and a fabricated `/api/boudica?prompt=...` endpoint shape that doesn't match any real backend command.

### 12.7 The storefront's Boudica chat had no idea what's actually in stock — found by the user actually using it

Caught live by the user, not found by reading code: asking the storefront's chat (`index.html`'s modal, `js/epos.js`) "do you have any of this in stock?" after it had walked through a craft-bear materials list got back an answer sending the customer to **Michaels, Jo-Ann Fabric, Hobby Lobby, Etsy, and Amazon** — recommending competitors instead of checking this store's own inventory. `AskBoudica()` called `getadvice` with the raw prompt and nothing else; `get_advice()` (`main.cpp`) is generic craft-advice prompting with zero catalog awareness.

Fixed the same way the kiosk's customer chat is grounded (§11): `epos.js` now extracts keywords from the question, looks up real matches via `getcatalog` (full message first, then significant individual words — same `extractKeywords()`/`findCatalogMatches()` approach), and — when it finds any — prepends them as plain-text context ("Here is what we currently have in stock that may be relevant...") before sending to `getadvice`. Deliberately plain text, not a JSON file attachment: `getadvice` goes through `call_boudica()` (a plain single-message call), not `call_boudica_with_data()`'s multipart path, and the kiosk's customer-facing chat already proved plain inline text works fine for this — the inline-data problem in §11.1/§12.6 was specific to a structured JSON-selection task, not natural-language grounding context.

**Live-verified** against the real running stack: asking about "yarn, stuffing, felt" now correctly lists the real in-stock Aran Wool and Polyester Stuffing with real prices and stock status, and correctly states felt isn't in inventory rather than inventing an answer — pulled from the running frontend's actual served `epos.js`, executed for real under Node against the live backend, same method as the rest of §12.

### Not attempted / explicitly deferred (per the user's own sequencing choice)

The special-order form still only `console.log`s and shows a fake success toast — wiring it to the real `addspecialorder` command needs form fields (order number, product list, deposit amount) this simple email+details form doesn't collect; that's storefront-completeness work (goal 2), not a security fix. Three different fictional brand names across `index.html` (title/nav/footer) and the receipt's hardcoded "The Curiosity Cabins" branding are cosmetic, deferred to the marketing-site goal (goal 1). A commented-out, abandoned Discord-widget embed was found in `index.html`'s news modal — a real external Discord server ID, inert, not activated — noted as a possible breadcrumb for the community goal (goal 4), not acted on.

**Live-verified throughout**, not just read: every backend command this directory calls was exercised directly against the real running stack (`getcatalog`, `validatecart`, `webstoreorder`, `orderhistory`, `getreceipt`, `getadvice`, `getpublicconfig`, `login`), and the actual served `js/session.js`/`data.js` files were fetched from the running frontend container and executed for real under Node (with minimal `sessionStorage` polyfilling) against the live backend — not just syntax-checked. No browser was available this session, so the visual result and the DOM-dependent `escapeHtml()` path itself weren't click-tested.
